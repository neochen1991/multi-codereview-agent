# 影响报告模板

生成报告是，按以下模板填充内容。`{{ }}` 为占位符，替换为实际分析结果。

---

## 报告模板

```markdown
## 代码关联影响分析报告

> 分析时间：{{ 当前时间 }}
> 对比分支：{{ source_branch }} -> {{ target_branch }}

---

### 变更概要

｜ 维度 ｜ 数量 ｜
｜-----｜----- ｜
｜变更Java文件 ｜ {{ java_file_count }} |
｜变更MyBatis XML ｜ {{ xml_file_count }} |
｜受影响方法 ｜ {{ method_count }} |
｜受影响SQL映射 ｜ {{ sql_count }} |

### 变更方法清单

#### Java 方法

｜ 类名 ｜ 方法 ｜ 变更类型 ｜ 文件 ｜
｜ ----｜ ---- ｜------｜ ------｜
{{ 遍历 java_methods，每行一条记录 }}

#### MyBatis SQL 映射

｜ Namespace ｜ SQL ID ｜ 类型 ｜ 变更类型 ｜ 文件 ｜
｜ ----------｜ -------｜ ---- ｜ -------｜ -----｜
{{ 遍历 mybatis_sqls，每行一条记录 }}

### 调用链影响分析

{{ 遍历 call_chains，每个方法生成以下块 }}

#### `{{ method_signature }}`

** 上游调用者（受影响）：**
{{ 列出 callers，无则显示 “无上游调用”}}

** 下游调用者（受影响）：**
{{ 列出 callees，无则显示 “无下游调用”}}

** 影响说明：**
{{ 基于调用链分析，简要说明该方法变更可能带来的影响 }}

---

{{ 结束遍历 }}

### 风险评估

｜ 风险等级 ｜ 说明 ｜
｜ --------｜------｜
｜ 高风险 ｜ {{ 列出高风险项：如修改了被多处调用的核心方法 }} ｜
｜ 中风险 ｜ {{ 列出中风险项：如修改了有上下游依赖的方法 }} ｜
｜ 低风险 ｜ {{ 列出低风险项：如新增方法、无调用链影响 }} ｜

### 测试建议

基于影响分析，建议关注以下测试场景：

{{ 根据调用链和变更类型，列出需要重点测试的场景 }}

1. {{ 测试场景 1 }}
2. {{ 测试场景 2 }}
3 ...

### 分析异常

{{ 如果 errors 数组非空，列出查询失败的方法 }}
{{ 如果无异常，显示“无异常”}}
```

---

## 风险等级判定规则

- **高风险** ：方法被3个以上调用者引用，或涉及数据写入操作（insert/update/delete）
- **中风险** ：方法有上下游调用链，或修改了MyBatis SQL逻辑
- **低风险** ：新增方法无调用者，或仅修改了注释/日志
