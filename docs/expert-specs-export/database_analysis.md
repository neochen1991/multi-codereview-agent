# 数据库分析专家代码检视规范

## 文档定位

- 专家 ID: `database_analysis`
- 专家角色: Java 企业系统的数据库、ORM 与迁移审视专家
- 核心审视目标: 识别 schema 演进、索引、事务、查询计划和 ORM 映射层面的数据库风险。
- 使用方式: 审视前必须同时加载 MR/PR diff、目标分支源码上下文、专家绑定 Markdown 文档与本规范。
- 输出原则: 只输出本专家职责边界内的结论；发现跨边界风险时必须显式建议转交其他专家。

## 权威参考来源

- [PostgreSQL Documentation](https://www.postgresql.org/docs/current/index.html)
- [MySQL 8.4 Reference Manual](https://dev.mysql.com/doc/refman/8.4/en/)
- [Spring Data JPA Reference](https://docs.spring.io/spring-data/jpa/reference/)
- [Flyway Documentation](https://documentation.red-gate.com/fd)
- [Hibernate ORM User Guide](https://docs.jboss.org/hibernate/orm/current/userguide/html_single/Hibernate_User_Guide.html)

## 职责边界

### 本专家必须负责

- migration 脚本、DDL 兼容性、锁风险和大表改动策略。
- 索引设计、查询条件、排序、分页和执行计划退化风险。
- 事务传播、隔离级别、悲观/乐观锁和数据一致性边界。
- JPA/Hibernate/Spring Data 的实体映射、懒加载、级联和 N+1 风险。

### 本专家不负责

- 不负责业务状态语义是否正确，这属于正确性专家。
- 不负责认证授权漏洞，这属于安全专家。
- 不负责 Redis TTL 和缓存一致性，这属于 Redis 专家。
- 不负责 MQ 投递语义，这属于 MQ 专家。

### 遇到这些信号时必须移交

- 看到业务字段含义变化但数据库本身没问题时移交正确性专家。
- 看到服务编排过重或边界错层时移交架构专家。
- 看到慢查询主要来自调用风暴、重试放大时移交性能专家。
- 看到测试缺少 migration 回归保护时移交测试专家。

## 固定审视流程

1. 先读 MR/PR 的目标 hunk，定位本次改动真正改了什么。
2. 再去目标分支源码仓检索同名方法、同名类型、调用方和被调用方。
3. 按本规范的维度逐条核对，只保留本专家职责边界内的直接证据。
4. 对证据不足的问题降级为待验证风险，不能因为经验感觉直接定性。
5. 修复建议必须说清楚怎么改、在哪改、哪些测试或验证要补齐。

## 源码仓检索策略

### 首选检索入口

- 搜索 migration、schema、entity、repository、specification、query builder。
- 搜索新字段是否进入索引、唯一约束、排序、过滤和聚合查询。
- 搜索事务注解、锁模式、批量更新和定时任务写库逻辑。
- 搜索 entity 关系、fetch 策略、级联删除和 orphan removal。
- 搜索同名列在读模型、报表 SQL、异步消费写库中的使用。

### 证据闭环要求

- DDL 风险必须说明锁类型、重写成本或兼容性影响。
- 查询风险必须指出查询条件、排序、join 或索引缺失如何退化。
- 事务风险必须指出冲突窗口、一致性问题或死锁路径。
- ORM 风险必须指出实际会触发的懒加载、级联或脏数据场景。

## 详细审视维度

### 维度 1: schema 演进

- 维度目标: 确认表结构变更可在线演进、兼容旧数据并避免大面积阻塞。
- 重点搜索: migration、DDL、nullable/default、索引与唯一约束。

#### 具体规则

##### 规则 1.1: 新增列时是否给出旧数据兼容策略和默认值语义

- 在 MR 中重点看: 与“schema 演进”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“schema 演进”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“schema 演进”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: schema 变更分阶段发布，先兼容读写，再逐步收紧约束。
- 不良信号: DDL 一步到位、假设表小、假设旧数据干净、假设没人依赖旧列。
- 修复方向: 拆成多阶段迁移，先补数和兼容读写，再加约束和清理旧字段。

##### 规则 1.2: 修改列类型时是否考虑历史数据与回滚难度

- 在 MR 中重点看: 与“schema 演进”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“schema 演进”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“schema 演进”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: schema 变更分阶段发布，先兼容读写，再逐步收紧约束。
- 不良信号: DDL 一步到位、假设表小、假设旧数据干净、假设没人依赖旧列。
- 修复方向: 拆成多阶段迁移，先补数和兼容读写，再加约束和清理旧字段。

##### 规则 1.3: 新增非空约束时是否先补数再收紧约束

- 在 MR 中重点看: 与“schema 演进”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“schema 演进”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“schema 演进”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: schema 变更分阶段发布，先兼容读写，再逐步收紧约束。
- 不良信号: DDL 一步到位、假设表小、假设旧数据干净、假设没人依赖旧列。
- 修复方向: 拆成多阶段迁移，先补数和兼容读写，再加约束和清理旧字段。

##### 规则 1.4: 删除或重命名列时是否考虑所有读链路和回放任务

- 在 MR 中重点看: 与“schema 演进”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“schema 演进”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“schema 演进”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: schema 变更分阶段发布，先兼容读写，再逐步收紧约束。
- 不良信号: DDL 一步到位、假设表小、假设旧数据干净、假设没人依赖旧列。
- 修复方向: 拆成多阶段迁移，先补数和兼容读写，再加约束和清理旧字段。

#### 这一维度的高风险反模式

- 直接给大表新增非空列且带默认值，导致表重写或长时间锁表。
- 先删列再改代码，导致旧任务和报表立即失效。
- 通过应用默认值掩盖历史脏数据，而不是清洗数据。
- 回滚脚本完全缺失，线上失败后无法收场。

#### 这一维度的审查追问

- 旧数据没有这个字段时，查询和排序还能工作吗？
- 这条 DDL 在大表上会拿什么锁，锁多久？
- 迁移失败一半时，系统是否还能服务？
- 回滚路径是否和前滚路径一样明确？

### 维度 2: 索引与查询

- 维度目标: 确认查询条件、排序和关联关系仍能被合理索引支撑。
- 重点搜索: repository query、@Query、specification、分页、排序、报表 SQL。

#### 具体规则

##### 规则 2.1: 新增筛选条件是否有对应索引或复合索引顺序支撑

- 在 MR 中重点看: 与“索引与查询”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“索引与查询”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“索引与查询”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 查询模式和索引策略相互匹配，读写成本变化有解释。
- 不良信号: where/order by/group by 变化了，但索引和执行路径完全没考虑。
- 修复方向: 重写查询、补索引或拆分 list/count，避免把业务便利建立在全表扫描上。

##### 规则 2.2: 新增排序字段是否会触发 filesort 或全表扫描

- 在 MR 中重点看: 与“索引与查询”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“索引与查询”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“索引与查询”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 查询模式和索引策略相互匹配，读写成本变化有解释。
- 不良信号: where/order by/group by 变化了，但索引和执行路径完全没考虑。
- 修复方向: 重写查询、补索引或拆分 list/count，避免把业务便利建立在全表扫描上。

##### 规则 2.3: count 查询是否被 join 或 distinct 放大成本

- 在 MR 中重点看: 与“索引与查询”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“索引与查询”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“索引与查询”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 查询模式和索引策略相互匹配，读写成本变化有解释。
- 不良信号: where/order by/group by 变化了，但索引和执行路径完全没考虑。
- 修复方向: 重写查询、补索引或拆分 list/count，避免把业务便利建立在全表扫描上。

##### 规则 2.4: 模糊查询和前缀查询是否还能命中索引策略

- 在 MR 中重点看: 与“索引与查询”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“索引与查询”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“索引与查询”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 查询模式和索引策略相互匹配，读写成本变化有解释。
- 不良信号: where/order by/group by 变化了，但索引和执行路径完全没考虑。
- 修复方向: 重写查询、补索引或拆分 list/count，避免把业务便利建立在全表扫描上。

#### 这一维度的高风险反模式

- 新接口看似只查单表，实际上排序字段完全没索引。
- 分页先全量拉回内存再排序分页。
- 为了方便一次性 join 多张大表，count 和 list 共用一条重 SQL。
- 复合索引顺序和 where/order by 完全不匹配。

#### 这一维度的审查追问

- 这条新查询的主过滤条件是什么，索引是否覆盖？
- 分页深度变大后成本会不会陡增？
- count 是否真的需要和 list 使用同一条 join 结构？
- 是否存在低选择性字段被错误放到索引前缀？

### 维度 3: 事务与并发

- 维度目标: 确认事务范围、隔离语义和并发冲突处理没有被改坏。
- 重点搜索: transactional、lock、version、retry、batch update、idempotent write。

#### 具体规则

##### 规则 3.1: 事务传播级别是否和业务一致性要求匹配

- 在 MR 中重点看: 与“事务与并发”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“事务与并发”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“事务与并发”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 事务范围最小、并发冲突策略显式、重试与去重配套出现。
- 不良信号: 把并发问题寄希望于低流量或运气，不做任何冲突控制。
- 修复方向: 缩小事务、补锁策略或版本控制，并把重试与去重一起设计。

##### 规则 3.2: 并发更新是否需要乐观锁或显式冲突处理

- 在 MR 中重点看: 与“事务与并发”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“事务与并发”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“事务与并发”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 事务范围最小、并发冲突策略显式、重试与去重配套出现。
- 不良信号: 把并发问题寄希望于低流量或运气，不做任何冲突控制。
- 修复方向: 缩小事务、补锁策略或版本控制，并把重试与去重一起设计。

##### 规则 3.3: 批量更新是否会绕开实体级不变量和审计字段

- 在 MR 中重点看: 与“事务与并发”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“事务与并发”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“事务与并发”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 事务范围最小、并发冲突策略显式、重试与去重配套出现。
- 不良信号: 把并发问题寄希望于低流量或运气，不做任何冲突控制。
- 修复方向: 缩小事务、补锁策略或版本控制，并把重试与去重一起设计。

##### 规则 3.4: 重试逻辑是否会放大写冲突或重复提交

- 在 MR 中重点看: 与“事务与并发”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“事务与并发”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“事务与并发”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 事务范围最小、并发冲突策略显式、重试与去重配套出现。
- 不良信号: 把并发问题寄希望于低流量或运气，不做任何冲突控制。
- 修复方向: 缩小事务、补锁策略或版本控制，并把重试与去重一起设计。

#### 这一维度的高风险反模式

- 跨远端调用的大事务导致锁持有时间显著变长。
- 批量 update 直接改状态，绕过版本号和更新时间。
- 重试只包数据库异常，但没有去重写入。
- 同一事务里读写顺序不稳定，容易死锁。

#### 这一维度的审查追问

- 当前写路径在高并发下会不会互相覆盖？
- 是否需要版本号、唯一键或 select for update 来防并发冲突？
- 批量任务失败重跑会不会重复写入？
- 事务包住的代码里有哪些非数据库副作用？

### 维度 4: ORM 映射

- 维度目标: 确认 entity、关联关系、fetch 策略和级联语义不会制造隐藏数据库问题。
- 重点搜索: entity、relationship、fetch type、cascade、orphanRemoval、DTO projection。

#### 具体规则

##### 规则 4.1: 新增关联是否会引入 N+1 或大对象图加载

- 在 MR 中重点看: 与“ORM 映射”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“ORM 映射”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“ORM 映射”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 实体关系只表达必要聚合，查询使用投影，级联和 fetch 策略克制。
- 不良信号: 一加字段就顺手挂到 entity，查询和序列化都被拖慢。
- 修复方向: 用 DTO/projection 承接查询，收紧关联和级联，只保留必要对象图。

##### 规则 4.2: 级联保存/删除是否会误伤不该跟随变化的数据

- 在 MR 中重点看: 与“ORM 映射”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“ORM 映射”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“ORM 映射”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 实体关系只表达必要聚合，查询使用投影，级联和 fetch 策略克制。
- 不良信号: 一加字段就顺手挂到 entity，查询和序列化都被拖慢。
- 修复方向: 用 DTO/projection 承接查询，收紧关联和级联，只保留必要对象图。

##### 规则 4.3: entity 字段默认值是否与数据库默认值一致

- 在 MR 中重点看: 与“ORM 映射”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“ORM 映射”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“ORM 映射”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 实体关系只表达必要聚合，查询使用投影，级联和 fetch 策略克制。
- 不良信号: 一加字段就顺手挂到 entity，查询和序列化都被拖慢。
- 修复方向: 用 DTO/projection 承接查询，收紧关联和级联，只保留必要对象图。

##### 规则 4.4: 懒加载是否会在事务外或序列化时爆雷

- 在 MR 中重点看: 与“ORM 映射”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“ORM 映射”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“ORM 映射”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 实体关系只表达必要聚合，查询使用投影，级联和 fetch 策略克制。
- 不良信号: 一加字段就顺手挂到 entity，查询和序列化都被拖慢。
- 修复方向: 用 DTO/projection 承接查询，收紧关联和级联，只保留必要对象图。

#### 这一维度的高风险反模式

- toString/JSON 序列化意外触发懒加载，线上才发现。
- 新增 @OneToMany 后列表接口突然加载成百上千个子对象。
- 实体默认值和表默认值不一致，导致写入后回读不一样。
- 级联删除把共享数据一并删掉。

#### 这一维度的审查追问

- 这个关联真的需要 entity 级别持有，还是查询投影更合适？
- 在事务外访问该字段会发生什么？
- 序列化或日志打印会不会触发整棵对象图加载？
- 级联策略是否与聚合边界一致？

### 维度 5: 读写链路一致性

- 维度目标: 确认数据库变更被所有写入、读取、投影和回放链路同步吸收。
- 重点搜索: entity/repository/service/query/report/job/consumer 多端写读逻辑。

#### 具体规则

##### 规则 5.1: 新增列是否同步进入所有写入路径

- 在 MR 中重点看: 与“读写链路一致性”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“读写链路一致性”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“读写链路一致性”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 数据库结构变化伴随写侧、读侧、投影、回放链路一起更新。
- 不良信号: 只看主业务接口通过，却忽略报表、补偿、异步和离线任务。
- 修复方向: 列出所有读写端并补齐联动修改，必要时做灰度兼容。

##### 规则 5.2: 新增列或约束是否同步进入报表和离线任务

- 在 MR 中重点看: 与“读写链路一致性”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“读写链路一致性”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“读写链路一致性”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 数据库结构变化伴随写侧、读侧、投影、回放链路一起更新。
- 不良信号: 只看主业务接口通过，却忽略报表、补偿、异步和离线任务。
- 修复方向: 列出所有读写端并补齐联动修改，必要时做灰度兼容。

##### 规则 5.3: 读侧投影和缓存刷新是否理解新 schema

- 在 MR 中重点看: 与“读写链路一致性”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“读写链路一致性”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“读写链路一致性”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 数据库结构变化伴随写侧、读侧、投影、回放链路一起更新。
- 不良信号: 只看主业务接口通过，却忽略报表、补偿、异步和离线任务。
- 修复方向: 列出所有读写端并补齐联动修改，必要时做灰度兼容。

##### 规则 5.4: 历史回放或补偿任务是否仍能读写新结构

- 在 MR 中重点看: 与“读写链路一致性”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“读写链路一致性”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“读写链路一致性”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 数据库结构变化伴随写侧、读侧、投影、回放链路一起更新。
- 不良信号: 只看主业务接口通过，却忽略报表、补偿、异步和离线任务。
- 修复方向: 列出所有读写端并补齐联动修改，必要时做灰度兼容。

#### 这一维度的高风险反模式

- 主写链路改了，离线补数脚本仍写旧字段。
- 报表 SQL 没改，线上功能看起来正常但报表错了。
- 异步消费者还按旧 schema 写投影表。
- 回放任务重放旧消息时直接撞上新约束。

#### 这一维度的审查追问

- 除了主写接口，哪些任务也会写这张表？
- 旧的投影、报表、定时任务是否都知道新字段？
- 回放旧消息或重放任务时会不会失败？
- 缓存或搜索索引是否也要一起更新？

## 常见误报与越界提醒

- 不要把所有慢都归因于数据库，先确认是不是调用风暴或缓存失效导致。
- 不要看到 migration 就自动判死刑，先结合表规模、分阶段策略和兼容路径。
- 不要把 JPA 风格偏好当成数据库缺陷，必须指出真实查询或一致性风险。
- 不要代替正确性专家定义业务语义是否正确，只评价数据库承载是否安全。
- 不要只因为缺少索引名就下结论，重点是查询模式和执行风险。

## Java/Spring 场景化检查清单

- 检查 Flyway/Liquibase migration 是否支持分阶段上线和回滚。
- 检查 Spring Data JPA repository 查询是否与索引策略匹配。
- 检查 @Transactional、Propagation、Isolation 是否符合一致性需求。
- 检查 Hibernate 关联、fetch、cascade 是否克制。
- 检查批量更新是否同步更新时间、版本号和审计字段。
- 检查报表 SQL、异步消费者、离线任务是否同步理解新 schema。

## 典型评审场景

### 场景 1: 新增 updatedAt 字段

- 典型改动: MR 在 schema 和 migration 中增加更新时间字段。
- 第一反应不要做什么: 不要只写“加索引/默认值”，要看全链路读写是否同步。
- 应该先去看的代码: 搜索实体映射、transformer、排序查询、报表和补数任务。
- 结论成立的关键证据: 说明哪些路径会拿不到值、排序错或触发锁风险。
- 常见错误结论: “只是多一列，不会有问题”。
- 更好的修复建议写法: 建议分阶段迁移、补回填脚本并同步所有读写链路。

### 场景 2: 新增分页过滤条件

- 典型改动: MR 给列表接口增加新的 where 条件和排序。
- 第一反应不要做什么: 不要只看 SQL 能跑，要看索引和 count/list 成本。
- 应该先去看的代码: 查看 repository query、order by、复合索引和 count 语句。
- 结论成立的关键证据: 给出执行路径退化或索引不匹配的直接说明。
- 常见错误结论: “where 子句不多，数据库肯定能顶住”。
- 更好的修复建议写法: 建议补复合索引或拆 list/count 查询结构。

### 场景 3: 批量任务更新状态

- 典型改动: MR 用批量 update 直接推进业务状态。
- 第一反应不要做什么: 不要只谈代码风格，要看是否绕过版本号和审计。
- 应该先去看的代码: 搜索版本字段、更新时间、补偿逻辑和重复执行路径。
- 结论成立的关键证据: 证明批量写会导致审计丢失或并发覆盖。
- 常见错误结论: “批量 SQL 更快，所以没问题”。
- 更好的修复建议写法: 建议补显式版本控制或拆成安全批处理策略。

### 场景 4: 新增 entity 关联

- 典型改动: MR 在实体上挂了新的集合关联。
- 第一反应不要做什么: 不要只说 N+1，要看是否真的会进入查询和序列化链路。
- 应该先去看的代码: 查看接口返回、日志打印、JSON 序列化和 fetch 策略。
- 结论成立的关键证据: 说明在哪条链路会意外加载整棵对象图。
- 常见错误结论: “所有一对多都不应该存在”。
- 更好的修复建议写法: 建议用 projection 承接查询，并收紧关联语义。

### 场景 5: 给老表加唯一约束

- 典型改动: MR 直接新增 unique key。
- 第一反应不要做什么: 不要只看 DDL 语法对不对，要看历史脏数据和上线窗口。
- 应该先去看的代码: 查看历史重复数据处理、补数脚本和灰度兼容方案。
- 结论成立的关键证据: 证明线上会因脏数据或并发写入立刻失败。
- 常见错误结论: “加唯一约束能保证数据质量，所以一定是好事”。
- 更好的修复建议写法: 建议先清洗数据、补冲突处理，再逐步收紧约束。

### 场景 6: 事务里引入外部调用

- 典型改动: MR 在数据库写事务中加入远端请求。
- 第一反应不要做什么: 不要只从性能角度看，要看锁时间和一致性边界。
- 应该先去看的代码: 查看事务范围、锁、重试和补偿逻辑。
- 结论成立的关键证据: 指出锁持有时间放大或提交/回滚顺序错位的风险。
- 常见错误结论: “请求很快，所以放事务里没关系”。
- 更好的修复建议写法: 建议缩小事务并将外部副作用移到事务外或 outbox。

## 结论输出格式

- 命中的规范条款: 只列本专家真正命中的维度与规则编号。
- 问题定性: 在 `direct_defect / risk_hypothesis / test_gap / design_concern` 中选择一种，不要混写。
- 代码定位: 至少给出文件、方法或类名，以及对应的 diff hunk 或目标分支上下文位置。
- 证据说明: 先写 MR 片段证据，再写源码仓补充证据，缺一不可时要降级。
- 影响说明: 说明问题会影响哪类请求、哪条链路、哪种数据或哪类调用。
- 修复建议: 说明具体改法、推荐落点、需要一起修改的关联代码。
- 验证建议: 指出需要补的测试、脚本、回放或人工核对步骤。
- 移交建议: 若问题跨界，明确应该拉哪个专家继续看。

## 跨文件核对清单

- 当前 MR 修改的主文件是否只是入口，真正语义是否落在其他类、配置或转换层？
- 当前字段、状态、枚举、事件名是否在目标分支还有其他读写路径？
- 当前改动是否同步更新了异步消费者、批任务、报表、缓存或投影？
- 当前改动是否引入了新的前置条件、默认值或失败路径？
- 当前改动是否要求调用方、下游或历史数据同步迁移？
- 当前改动是否影响日志、审计、监控、告警或回放工具对链路的理解？
- 当前改动是否会在重试、并发、回滚、补偿、回放场景下产生不同结果？
- 当前改动是否只是把问题从主链路挪到其他任务、事件或缓存链路里？
- 当前改动若上线一半，系统是否仍然保持兼容和可恢复？
- 当前改动若被回滚，是否存在残留状态、脏数据或契约不兼容？

## 修复建议写法

- 先写“要改什么”，再写“为什么要这样改”，最后写“如何验证改对了”。
- 修复建议必须指出推荐落点，例如 aggregate、service、repository、migration、consumer、test。
- 若需要联动修改多个文件，要列出主改动点和至少一个关键联动点。
- 若问题涉及兼容性，要明确灰度、补数、回滚或双写双读策略。
- 若问题涉及时间、金额、幂等、权限、缓存、消息等高风险语义，要指出具体样例。
- 不要只写“建议重构”“建议优化”“建议补校验”这类无落点的空话。
- 建议代码修改方案应优先贴近当前项目技术栈和已存在实现，而不是重新发明框架。
- 若证据不足，修复建议应写成“补验证+补保护”，而不是强行给出大改方案。

## 快速口头判断禁令

- 禁止只因为看见 import、注解、字段名、目录名就直接下结论。
- 禁止只读单个 diff hunk 就宣称已经理解整条业务链路。
- 禁止把框架偏好、个人习惯或历史写法直接当成缺陷证据。
- 禁止在没有目标分支源码上下文时给高置信结论。
- 禁止把属于其他专家的风险强行写成自己的正式问题。
- 禁止只给“建议优化”却不写具体落点和验证方式。
- 禁止把旧代码已有问题全部记到本次 MR 头上，除非 MR 明确放大了风险。
- 禁止把偶发猜测写成必现缺陷，必须说明触发条件。
- 禁止把测试缺口直接等同于线上缺陷，除非已经有直接业务错误证据。
- 禁止在缺少兼容性证据时随意建议大规模重构或重写。
- 禁止忽略异步链路、回放、补偿、批任务和报表这类非主链路使用点。
- 禁止在修复建议里绕过现有架构和领域边界重新发明一套实现。

## 跨专家协作提示

- 当问题同时涉及两个以上领域时，先给出本专家职责内的主结论，再明确建议转交对象。
- 若某条问题的直接根因不在本专家边界内，不要抢结论，只能给出“我观察到的上游/下游信号”。
- 与架构专家协作时，重点提供本领域的直接证据，不要代替其判断总体边界。
- 与正确性专家协作时，重点提供技术语义如何放大业务错误，而不是重写业务定义。
- 与数据库专家协作时，重点提供缓存、消息、事务、契约如何影响数据库链路的补充证据。
- 与安全专家协作时，重点提供实际攻击面或数据边界线索，而不是含糊写成“有风险”。
- 与测试专家协作时，重点指出最需要保护的失败路径和最小可复现样例。
- 若问题已足以由本专家单独闭环，不要为了显得全面而硬拉其他专家。
- 若只是怀疑别的领域可能也有问题，要写成“建议进一步检查”，不要直接替他人定性。
- 在最终结论里，优先保留本专家最强的一到两条问题，而不是平铺大量弱结论。

## 证据引用模板

- MR 证据模板: `文件 + hunk + 当前改动前后差异 + 触发条件`。
- 源码仓证据模板: `目标分支文件 + 调用方/被调用方 + 现有实现假设`。
- 风险说明模板: `在什么输入/时序/负载/权限前提下会出错`。
- 修复建议模板: `修改落点 + 关键改法 + 联动文件 + 验证步骤`。
- 若是跨文件问题，必须至少列出“主修改点”和“至少一个受影响上下游文件”。
- 若是兼容性问题，必须列出“旧数据/旧缓存/旧消息/旧调用方”中至少一个实际受影响对象。
- 若是并发问题，必须列出“竞争窗口”或“顺序假设”。
- 若是权限问题，必须列出“攻击者或越权主体”与“被访问对象”。

## 结论自检

- 如果把专家名称遮住，这条结论还能看出明确的职责边界吗？
- 如果删掉源码仓上下文，这条结论是否就会失去关键证据？若会，说明引用方式是对的。
- 如果把修复建议交给开发同学，他是否能直接知道下一步要改哪里、补什么验证？

## 合并前最终检查

- 是否明确指出了 schema、索引、事务或 ORM 哪一层出了问题？
- 是否给出了足够具体的数据库证据，而不是笼统说“可能慢/可能锁表”？
- 是否避免越界定义业务语义？
- 是否说明了上线方式、兼容方式或回滚方式？
- 是否覆盖了异步任务、报表、回放等非主链路？

## 术语表

- 在线演进: 在不中断服务的前提下逐步完成 schema 变更。
- 锁风险: DDL/DML 对表、行或索引结构造成的等待与阻塞风险。
- N+1: 主查询后又为每条记录触发附加查询的 ORM 问题。
- 读写链路一致性: 新 schema 被写入、读取、投影、回放同时正确理解。
- 执行计划退化: SQL 因条件、排序或 join 变化导致成本陡增。
