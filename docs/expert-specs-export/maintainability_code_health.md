# 可维护性与代码健康专家代码检视规范

## 文档定位

- 专家 ID: `maintainability_code_health`
- 专家角色: 关注复杂度、重复、可读性和演进成本的代码健康审视专家
- 核心审视目标: 识别让代码难读、难改、难测、难复用的结构性可维护性问题。
- 使用方式: 审视前必须同时加载 MR/PR diff、目标分支源码上下文、专家绑定 Markdown 文档与本规范。
- 输出原则: 只输出本专家职责边界内的结论；发现跨边界风险时必须显式建议转交其他专家。

## 权威参考来源

- [Google Java Style Guide](https://google.github.io/styleguide/javaguide.html)
- [Refactoring by Martin Fowler](https://martinfowler.com/books/refactoring.html)
- [Clean Code Summary](https://blog.cleancoder.com/)

## 职责边界

### 本专家必须负责

- 方法和类复杂度、重复逻辑、命名、职责散布、局部可读性。
- 可演进性、局部抽象质量、边界内重复实现和坏味道累积。
- 可测试性前提，例如隐藏副作用、难注入依赖和难构造输入。
- 异常处理、日志结构、配置读取方式对可维护性的影响。

### 本专家不负责

- 不负责业务语义是否正确，这属于正确性专家。
- 不负责架构边界和上下文设计，这属于架构或 DDD 专家。
- 不负责安全定性，这属于安全专家。
- 不负责性能瓶颈归因，这属于性能专家。

### 遇到这些信号时必须移交

- 看到分层越界或模块耦合时移交架构专家。
- 看到业务状态或字段语义错误时移交正确性专家。
- 看到测试覆盖缺口时移交测试专家。
- 看到数据库、Redis、MQ 细节时移交对应基础设施专家。

## 固定审视流程

1. 先读 MR/PR 的目标 hunk，定位本次改动真正改了什么。
2. 再去目标分支源码仓检索同名方法、同名类型、调用方和被调用方。
3. 按本规范的维度逐条核对，只保留本专家职责边界内的直接证据。
4. 对证据不足的问题降级为待验证风险，不能因为经验感觉直接定性。
5. 修复建议必须说清楚怎么改、在哪改、哪些测试或验证要补齐。

## 源码仓检索策略

### 首选检索入口

- 搜索同名逻辑是否在多个 service/handler/util 中重复。
- 搜索长方法、深层嵌套、布尔参数和分支复制。
- 搜索异常处理、日志模板、配置读取和常量散落位置。
- 搜索单测构造是否需要大量样板或很难注入依赖。
- 搜索 util/helper/static 入口是否演化成隐藏耦合点。

### 证据闭环要求

- 必须指出维护成本为什么会升高，而不是只说代码不好看。
- 重复逻辑必须至少指出两处或更多落点。
- 复杂度问题应给出嵌套、分支、依赖或副作用层面的具体证据。
- 能用小范围重构解决的问题，不要夸张为架构级缺陷。

## 详细审视维度

### 维度 1: 复杂度与职责切分

- 维度目标: 确认方法和类的复杂度没有膨胀到影响理解和修改安全性。
- 重点搜索: 长方法、深层 if/else、复杂条件、超大类、职责散布。

#### 具体规则

##### 规则 1.1: 方法是否承担了多个不同意图的步骤

- 在 MR 中重点看: 与“复杂度与职责切分”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“复杂度与职责切分”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“复杂度与职责切分”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 主路径清晰、职责单一、局部改动能被局部理解。
- 不良信号: 代码理解依赖作者脑补，稍改就担心连锁反应。
- 修复方向: 拆方法、提炼意图命名、消除 mode 参数、缩小类职责。

##### 规则 1.2: 条件分支是否过深且难以局部验证

- 在 MR 中重点看: 与“复杂度与职责切分”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“复杂度与职责切分”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“复杂度与职责切分”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 主路径清晰、职责单一、局部改动能被局部理解。
- 不良信号: 代码理解依赖作者脑补，稍改就担心连锁反应。
- 修复方向: 拆方法、提炼意图命名、消除 mode 参数、缩小类职责。

##### 规则 1.3: 一个类是否同时负责计算、装配、调用和错误处理

- 在 MR 中重点看: 与“复杂度与职责切分”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“复杂度与职责切分”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“复杂度与职责切分”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 主路径清晰、职责单一、局部改动能被局部理解。
- 不良信号: 代码理解依赖作者脑补，稍改就担心连锁反应。
- 修复方向: 拆方法、提炼意图命名、消除 mode 参数、缩小类职责。

##### 规则 1.4: 布尔参数或 mode 参数是否隐藏了多种职责

- 在 MR 中重点看: 与“复杂度与职责切分”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“复杂度与职责切分”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“复杂度与职责切分”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 主路径清晰、职责单一、局部改动能被局部理解。
- 不良信号: 代码理解依赖作者脑补，稍改就担心连锁反应。
- 修复方向: 拆方法、提炼意图命名、消除 mode 参数、缩小类职责。

#### 这一维度的高风险反模式

- 一个方法几十行到上百行，混合校验、编排、转换和副作用。
- 新增一个场景只能继续叠 if/else。
- 相同依赖在多个分支里反复调用，没人看得出主路径。
- 类名很抽象，但实际上什么都做。

#### 这一维度的审查追问

- 这段代码能否按“准备数据、核心决策、副作用提交”拆开？
- 是否存在一个参数在切换完全不同的行为？
- 局部改一个分支时，是否容易破坏其他分支？
- 读者能否在一分钟内看懂主路径？

### 维度 2: 重复与局部抽象

- 维度目标: 确认相同业务或技术意图没有以复制粘贴形式扩散。
- 重点搜索: 重复分支、重复 mapper、重复异常映射、重复日志模板、重复配置解析。

#### 具体规则

##### 规则 2.1: 相同业务规则是否在多个入口重复实现

- 在 MR 中重点看: 与“重复与局部抽象”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“重复与局部抽象”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“重复与局部抽象”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 重复被收敛到清晰可理解的抽象，而不是魔法基类。
- 不良信号: 每来一个新场景就复制一段旧代码改词。
- 修复方向: 抽出共享意图，保留必要差异点，避免过度抽象。

##### 规则 2.2: 相同转换逻辑是否存在多个轻微变体

- 在 MR 中重点看: 与“重复与局部抽象”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“重复与局部抽象”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“重复与局部抽象”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 重复被收敛到清晰可理解的抽象，而不是魔法基类。
- 不良信号: 每来一个新场景就复制一段旧代码改词。
- 修复方向: 抽出共享意图，保留必要差异点，避免过度抽象。

##### 规则 2.3: 相同异常映射和日志模板是否被手写复制

- 在 MR 中重点看: 与“重复与局部抽象”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“重复与局部抽象”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“重复与局部抽象”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 重复被收敛到清晰可理解的抽象，而不是魔法基类。
- 不良信号: 每来一个新场景就复制一段旧代码改词。
- 修复方向: 抽出共享意图，保留必要差异点，避免过度抽象。

##### 规则 2.4: 重复代码背后是否需要抽象而不是继续复制

- 在 MR 中重点看: 与“重复与局部抽象”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“重复与局部抽象”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“重复与局部抽象”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 重复被收敛到清晰可理解的抽象，而不是魔法基类。
- 不良信号: 每来一个新场景就复制一段旧代码改词。
- 修复方向: 抽出共享意图，保留必要差异点，避免过度抽象。

#### 这一维度的高风险反模式

- 复制一份旧分支改两个字段就上线。
- 每个 handler 各写一份几乎相同的校验和日志。
- 相同 mapper 在三个文件各写一版。
- 相同常量或配置键散落多个地方。

#### 这一维度的审查追问

- 如果这条规则还要再改一次，要改几个地方？
- 这几段相似代码真正共性的意图是什么？
- 抽象后会更清晰，还是只是隐藏差异？
- 哪些重复应该保留，哪些重复已经形成维护债？

### 维度 3: 命名与可读性

- 维度目标: 确认命名、日志、局部变量和注释能帮助维护者快速理解意图。
- 重点搜索: 类名、方法名、变量名、日志、异常信息、注释与 TODO。

#### 具体规则

##### 规则 3.1: 命名是否表达真实意图而不是技术细节

- 在 MR 中重点看: 与“命名与可读性”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“命名与可读性”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“命名与可读性”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 命名稳定、日志有上下文、注释只解释难点和原因。
- 不良信号: 读者需要来回跳文件才能猜出名字含义。
- 修复方向: 重命名、补上下文日志、删除无效注释并提炼意图方法。

##### 规则 3.2: 日志和异常是否能帮助定位问题而不是制造噪音

- 在 MR 中重点看: 与“命名与可读性”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“命名与可读性”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“命名与可读性”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 命名稳定、日志有上下文、注释只解释难点和原因。
- 不良信号: 读者需要来回跳文件才能猜出名字含义。
- 修复方向: 重命名、补上下文日志、删除无效注释并提炼意图方法。

##### 规则 3.3: 注释是否解释了为什么，而不是重复代码表面动作

- 在 MR 中重点看: 与“命名与可读性”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“命名与可读性”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“命名与可读性”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 命名稳定、日志有上下文、注释只解释难点和原因。
- 不良信号: 读者需要来回跳文件才能猜出名字含义。
- 修复方向: 重命名、补上下文日志、删除无效注释并提炼意图方法。

##### 规则 3.4: 局部变量是否帮助分段理解主路径

- 在 MR 中重点看: 与“命名与可读性”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“命名与可读性”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“命名与可读性”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 命名稳定、日志有上下文、注释只解释难点和原因。
- 不良信号: 读者需要来回跳文件才能猜出名字含义。
- 修复方向: 重命名、补上下文日志、删除无效注释并提炼意图方法。

#### 这一维度的高风险反模式

- 名字像 process/doHandle/commonData 这类看不出意图的命名。
- 异常日志全是 print stacktrace，没有业务上下文。
- 注释只是把代码再读一遍。
- 临时变量和布尔值命名完全依赖上下文猜测。

#### 这一维度的审查追问

- 这个名字是否能让新同事一眼知道它做什么？
- 日志里是否保留了足够的业务上下文和标识？
- 注释是否真的解释了约束和原因？
- 能否通过提炼变量或方法让代码自解释？

### 维度 4: 副作用可见性

- 维度目标: 确认副作用位置明显、顺序清楚，调用者不会被隐藏行为偷袭。
- 重点搜索: 隐式缓存写入、静态状态、全局配置、工具类副作用、隐式 IO。

#### 具体规则

##### 规则 4.1: 方法名是否掩盖了真实副作用

- 在 MR 中重点看: 与“副作用可见性”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“副作用可见性”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“副作用可见性”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 副作用通过命名、参数或返回值显式暴露。
- 不良信号: 方法看起来纯净，实际却藏着多个写操作。
- 修复方向: 显式化副作用、拆分纯计算与副作用提交、减少全局状态依赖。

##### 规则 4.2: 工具类或 helper 是否暗中写状态、发消息、打日志

- 在 MR 中重点看: 与“副作用可见性”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“副作用可见性”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“副作用可见性”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 副作用通过命名、参数或返回值显式暴露。
- 不良信号: 方法看起来纯净，实际却藏着多个写操作。
- 修复方向: 显式化副作用、拆分纯计算与副作用提交、减少全局状态依赖。

##### 规则 4.3: 是否存在隐式依赖系统时钟、线程上下文或全局变量

- 在 MR 中重点看: 与“副作用可见性”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“副作用可见性”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“副作用可见性”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 副作用通过命名、参数或返回值显式暴露。
- 不良信号: 方法看起来纯净，实际却藏着多个写操作。
- 修复方向: 显式化副作用、拆分纯计算与副作用提交、减少全局状态依赖。

##### 规则 4.4: 调用者是否能从签名和名称看出后果

- 在 MR 中重点看: 与“副作用可见性”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“副作用可见性”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“副作用可见性”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 副作用通过命名、参数或返回值显式暴露。
- 不良信号: 方法看起来纯净，实际却藏着多个写操作。
- 修复方向: 显式化副作用、拆分纯计算与副作用提交、减少全局状态依赖。

#### 这一维度的高风险反模式

- 看起来像 getter 的方法其实会写库或发消息。
- helper 内部偷偷依赖 ThreadLocal 和全局配置。
- 工具方法顺手刷缓存或发监控，没有暴露在接口上。
- 调用顺序稍变就引入隐藏副作用。

#### 这一维度的审查追问

- 调用这个方法的人是否知道它会产生哪些副作用？
- 副作用是否可以显式上提到调用链上层？
- 是否依赖隐式上下文而非显式参数？
- 能否通过接口命名或返回值让副作用更可见？

### 维度 5: 可测试性前提

- 维度目标: 确认代码结构没有让后续补测试变得异常昂贵。
- 重点搜索: 静态依赖、构造复杂度、时间依赖、随机性、IO 隐藏点。

#### 具体规则

##### 规则 5.1: 依赖是否容易注入和替换

- 在 MR 中重点看: 与“可测试性前提”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“可测试性前提”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“可测试性前提”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 核心逻辑易调用、依赖可替换、测试构造成本低。
- 不良信号: 任何测试都要起大半个系统或堆很多样板。
- 修复方向: 拆出纯逻辑、显式注入依赖、减少静态和隐式上下文。

##### 规则 5.2: 时间、随机数、外部 IO 是否可控

- 在 MR 中重点看: 与“可测试性前提”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“可测试性前提”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“可测试性前提”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 核心逻辑易调用、依赖可替换、测试构造成本低。
- 不良信号: 任何测试都要起大半个系统或堆很多样板。
- 修复方向: 拆出纯逻辑、显式注入依赖、减少静态和隐式上下文。

##### 规则 5.3: 核心逻辑是否能在无框架环境下被直接调用

- 在 MR 中重点看: 与“可测试性前提”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“可测试性前提”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“可测试性前提”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 核心逻辑易调用、依赖可替换、测试构造成本低。
- 不良信号: 任何测试都要起大半个系统或堆很多样板。
- 修复方向: 拆出纯逻辑、显式注入依赖、减少静态和隐式上下文。

##### 规则 5.4: 构造测试输入是否需要大量样板对象

- 在 MR 中重点看: 与“可测试性前提”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“可测试性前提”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“可测试性前提”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 核心逻辑易调用、依赖可替换、测试构造成本低。
- 不良信号: 任何测试都要起大半个系统或堆很多样板。
- 修复方向: 拆出纯逻辑、显式注入依赖、减少静态和隐式上下文。

#### 这一维度的高风险反模式

- 逻辑全写在 static 工具类里，很难替换依赖。
- 直接调用系统时钟、UUID、环境变量，测试不可预测。
- 测试一个分支需要堆十几层对象。
- 核心逻辑散落在注解驱动的回调里，难以单独触发。

#### 这一维度的审查追问

- 如果明天要补单测，这段代码最难测的点是什么？
- 是否可以把时间、随机性和 IO 提升成可注入依赖？
- 是否可以把核心逻辑从框架回调中提出来？
- 测试准备数据是否比真正断言还复杂？

## 常见误报与越界提醒

- 不要把个人风格差异当成可维护性缺陷，重点看维护成本是否真实升高。
- 不要把所有大方法都一刀切定性，先看复杂度是否真的影响理解和改动安全性。
- 不要把业务重复和架构重复混成一类，先界定重复发生在哪层。
- 不要越界判断业务正确性或性能问题。
- 不要把“代码短”误解为“代码易维护”。

## Java/Spring 场景化检查清单

- 检查 Spring 组件是否把核心逻辑写死在注解回调和静态工具里。
- 检查异常、日志、配置读取和 mapper 是否重复散落。
- 检查是否依赖系统时钟、UUID、环境变量等不可控输入。
- 检查构造器注入是否清晰表达真实依赖数量。
- 检查 util/common/helper 是否成为隐藏职责垃圾桶。

## 典型评审场景

### 场景 1: 复制旧分支改几个字段

- 典型改动: MR 为了新场景直接复制旧 handler/service。
- 第一反应不要做什么: 不要只说重复，要说明未来改动需要同步多少处。
- 应该先去看的代码: 搜索相似方法、相同日志和相同异常分支。
- 结论成立的关键证据: 指出至少两处重复代码和它们未来必然同步维护的证据。
- 常见错误结论: “复制也没关系，先能跑”。
- 更好的修复建议写法: 建议抽共享主路径，只保留场景差异点作为参数或策略。

### 场景 2: 方法过长且副作用混杂

- 典型改动: MR 在原有长方法上继续追加分支和写操作。
- 第一反应不要做什么: 不要只统计行数，要指出理解和测试为什么变难。
- 应该先去看的代码: 看主路径、副作用点和重复条件。
- 结论成立的关键证据: 说明局部改动无法局部验证的具体原因。
- 常见错误结论: “超过 50 行就是错”。
- 更好的修复建议写法: 建议按意图拆分并显式隔离副作用。

### 场景 3: 命名掩盖真实行为

- 典型改动: MR 新增一个名字像 query/get 的方法，实际会写状态。
- 第一反应不要做什么: 不要只说名字不好，要指出调用者会被误导。
- 应该先去看的代码: 看方法体里的写操作、缓存刷新、事件发布。
- 结论成立的关键证据: 指出调用点为什么会误判副作用。
- 常见错误结论: “重命名一下就行”。
- 更好的修复建议写法: 建议拆分纯读取与副作用操作，并重新命名接口。

### 场景 4: 测试构造成本飙升

- 典型改动: MR 引入更多静态调用和隐式依赖。
- 第一反应不要做什么: 不要替测试专家评价覆盖率，只看结构是否难测。
- 应该先去看的代码: 看是否依赖系统时钟、环境变量、静态单例。
- 结论成立的关键证据: 说明补一个简单测试需要哪些困难准备。
- 常见错误结论: “以后再补测试”。
- 更好的修复建议写法: 建议把不可控依赖抽出来，通过接口或参数注入。

### 场景 5: helper 吞掉关键语义

- 典型改动: MR 把一堆业务逻辑塞进 common helper。
- 第一反应不要做什么: 不要只说 helper 不好，要指出职责与调用可见性问题。
- 应该先去看的代码: 看 helper 被谁调用、依赖什么上下文、产生什么副作用。
- 结论成立的关键证据: 指出 helper 成为隐式业务中心的证据。
- 常见错误结论: “公共方法越多越方便”。
- 更好的修复建议写法: 建议把 helper 拆回明确所属模块或策略对象。

### 场景 6: 注释替代代码表达

- 典型改动: MR 通过大量注释解释复杂逻辑，但不做重构。
- 第一反应不要做什么: 不要一概反注释，要区分解释原因和掩盖复杂度。
- 应该先去看的代码: 看注释是否在替代更清晰的结构和命名。
- 结论成立的关键证据: 指出读者仍然无法仅凭代码理解主路径。
- 常见错误结论: “有注释就够了”。
- 更好的修复建议写法: 建议重构为自解释代码，仅保留解释原因与边界的注释。

## 结论输出格式

- 命中的规范条款: 只列本专家真正命中的维度与规则编号。
- 问题定性: 在 `direct_defect / risk_hypothesis / test_gap / design_concern` 中选择一种，不要混写。
- 代码定位: 至少给出文件、方法或类名，以及对应的 diff hunk 或目标分支上下文位置。
- 证据说明: 先写 MR 片段证据，再写源码仓补充证据，缺一不可时要降级。
- 影响说明: 说明问题会影响哪类请求、哪条链路、哪种数据或哪类调用。
- 修复建议: 说明具体改法、推荐落点、需要一起修改的关联代码。
- 验证建议: 指出需要补的测试、脚本、回放或人工核对步骤。
- 移交建议: 若问题跨界，明确应该拉哪个专家继续看。

## 跨文件核对清单

- 当前 MR 修改的主文件是否只是入口，真正语义是否落在其他类、配置或转换层？
- 当前字段、状态、枚举、事件名是否在目标分支还有其他读写路径？
- 当前改动是否同步更新了异步消费者、批任务、报表、缓存或投影？
- 当前改动是否引入了新的前置条件、默认值或失败路径？
- 当前改动是否要求调用方、下游或历史数据同步迁移？
- 当前改动是否影响日志、审计、监控、告警或回放工具对链路的理解？
- 当前改动是否会在重试、并发、回滚、补偿、回放场景下产生不同结果？
- 当前改动是否只是把问题从主链路挪到其他任务、事件或缓存链路里？
- 当前改动若上线一半，系统是否仍然保持兼容和可恢复？
- 当前改动若被回滚，是否存在残留状态、脏数据或契约不兼容？

## 修复建议写法

- 先写“要改什么”，再写“为什么要这样改”，最后写“如何验证改对了”。
- 修复建议必须指出推荐落点，例如 aggregate、service、repository、migration、consumer、test。
- 若需要联动修改多个文件，要列出主改动点和至少一个关键联动点。
- 若问题涉及兼容性，要明确灰度、补数、回滚或双写双读策略。
- 若问题涉及时间、金额、幂等、权限、缓存、消息等高风险语义，要指出具体样例。
- 不要只写“建议重构”“建议优化”“建议补校验”这类无落点的空话。
- 建议代码修改方案应优先贴近当前项目技术栈和已存在实现，而不是重新发明框架。
- 若证据不足，修复建议应写成“补验证+补保护”，而不是强行给出大改方案。

## 快速口头判断禁令

- 禁止只因为看见 import、注解、字段名、目录名就直接下结论。
- 禁止只读单个 diff hunk 就宣称已经理解整条业务链路。
- 禁止把框架偏好、个人习惯或历史写法直接当成缺陷证据。
- 禁止在没有目标分支源码上下文时给高置信结论。
- 禁止把属于其他专家的风险强行写成自己的正式问题。
- 禁止只给“建议优化”却不写具体落点和验证方式。
- 禁止把旧代码已有问题全部记到本次 MR 头上，除非 MR 明确放大了风险。
- 禁止把偶发猜测写成必现缺陷，必须说明触发条件。
- 禁止把测试缺口直接等同于线上缺陷，除非已经有直接业务错误证据。
- 禁止在缺少兼容性证据时随意建议大规模重构或重写。
- 禁止忽略异步链路、回放、补偿、批任务和报表这类非主链路使用点。
- 禁止在修复建议里绕过现有架构和领域边界重新发明一套实现。

## 跨专家协作提示

- 当问题同时涉及两个以上领域时，先给出本专家职责内的主结论，再明确建议转交对象。
- 若某条问题的直接根因不在本专家边界内，不要抢结论，只能给出“我观察到的上游/下游信号”。
- 与架构专家协作时，重点提供本领域的直接证据，不要代替其判断总体边界。
- 与正确性专家协作时，重点提供技术语义如何放大业务错误，而不是重写业务定义。
- 与数据库专家协作时，重点提供缓存、消息、事务、契约如何影响数据库链路的补充证据。
- 与安全专家协作时，重点提供实际攻击面或数据边界线索，而不是含糊写成“有风险”。
- 与测试专家协作时，重点指出最需要保护的失败路径和最小可复现样例。
- 若问题已足以由本专家单独闭环，不要为了显得全面而硬拉其他专家。
- 若只是怀疑别的领域可能也有问题，要写成“建议进一步检查”，不要直接替他人定性。
- 在最终结论里，优先保留本专家最强的一到两条问题，而不是平铺大量弱结论。

## 证据引用模板

- MR 证据模板: `文件 + hunk + 当前改动前后差异 + 触发条件`。
- 源码仓证据模板: `目标分支文件 + 调用方/被调用方 + 现有实现假设`。
- 风险说明模板: `在什么输入/时序/负载/权限前提下会出错`。
- 修复建议模板: `修改落点 + 关键改法 + 联动文件 + 验证步骤`。
- 若是跨文件问题，必须至少列出“主修改点”和“至少一个受影响上下游文件”。
- 若是兼容性问题，必须列出“旧数据/旧缓存/旧消息/旧调用方”中至少一个实际受影响对象。
- 若是并发问题，必须列出“竞争窗口”或“顺序假设”。
- 若是权限问题，必须列出“攻击者或越权主体”与“被访问对象”。

## 结论自检

- 如果把专家名称遮住，这条结论还能看出明确的职责边界吗？
- 如果删掉源码仓上下文，这条结论是否就会失去关键证据？若会，说明引用方式是对的。
- 如果把修复建议交给开发同学，他是否能直接知道下一步要改哪里、补什么验证？

## 合并前最终检查

- 是否明确指出了维护成本如何上升，而不是只做审美批评？
- 是否给出了重复、复杂度或隐藏副作用的直接证据？
- 是否避免越界去评价业务、安全、数据库问题？
- 是否给出了小步可实施的重构建议？
- 是否兼顾了可读性和可测试性两个维度？

## 术语表

- 代码健康: 代码被阅读、修改、测试和演进的综合成本。
- 隐藏副作用: 接口表面看不出来，但执行时会产生写操作或外部动作。
- 局部抽象: 围绕当前上下文提炼的最小有用抽象，而非全局框架化。
- 样板成本: 为了跑通或测试一小段逻辑而必须构造的大量无关代码。
