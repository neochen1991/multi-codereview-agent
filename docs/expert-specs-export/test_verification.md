# 测试与验证专家代码检视规范

## 文档定位

- 专家 ID: `test_verification`
- 专家角色: 关注测试分层、回归保护、断言质量和可验证性的测试专家
- 核心审视目标: 识别缺失测试、弱断言、错误夹具和无法保护回归的验证缺口。
- 使用方式: 审视前必须同时加载 MR/PR diff、目标分支源码上下文、专家绑定 Markdown 文档与本规范。
- 输出原则: 只输出本专家职责边界内的结论；发现跨边界风险时必须显式建议转交其他专家。

## 权威参考来源

- [JUnit 5 User Guide](https://junit.org/junit5/docs/current/user-guide/)
- [Spring Boot Testing Reference](https://docs.spring.io/spring-boot/docs/current/reference/html/features.html#features.testing)
- [Google Testing Blog](https://testing.googleblog.com/)

## 职责边界

### 本专家必须负责

- 单测、集成测试、契约测试、回归测试和夹具设计。
- 断言粒度、覆盖关键风险、异常路径与回归保护能力。
- 可测性前提，例如依赖注入、时间控制、数据构造和可观测输出。
- 测试是否真正覆盖这次 MR 改动的关键风险点。

### 本专家不负责

- 不直接定义业务语义是否正确，这属于正确性专家。
- 不负责架构边界，这属于架构专家。
- 不负责安全漏洞定性，这属于安全专家。
- 不负责数据库具体索引设计，这属于数据库专家。

### 遇到这些信号时必须移交

- 看到业务语义偏差时移交正确性专家。
- 看到可测性问题来自架构边界失真时移交架构或可维护性专家。
- 看到安全风险缺少安全测试时可附带建议并移交安全专家。
- 看到数据库迁移缺少验证时移交数据库专家给出主结论。

## 固定审视流程

1. 先读 MR/PR 的目标 hunk，定位本次改动真正改了什么。
2. 再去目标分支源码仓检索同名方法、同名类型、调用方和被调用方。
3. 按本规范的维度逐条核对，只保留本专家职责边界内的直接证据。
4. 对证据不足的问题降级为待验证风险，不能因为经验感觉直接定性。
5. 修复建议必须说清楚怎么改、在哪改、哪些测试或验证要补齐。

## 源码仓检索策略

### 首选检索入口

- 搜索与改动文件同名或同模块的 test/spec/integration 测试。
- 搜索异常路径、回归场景、时间/幂等/兼容性相关测试。
- 搜索夹具、builders、test data factory 和 mock/stub 使用方式。
- 搜索是否已有类似历史 bug 的回归测试模板可复用。

### 证据闭环要求

- 测试问题必须说明缺哪类保护，以及为什么这次改动需要它。
- 不能把所有没改测试的 MR 都判成问题，必须结合风险级别。
- 弱断言问题要指出测试虽存在但无法捕获此次回归。
- 可测性问题要说明为什么后续无法有效补测。

## 详细审视维度

### 维度 1: 风险覆盖

- 维度目标: 确认这次改动的关键风险点有对应测试保护。
- 重点搜索: unit/integration/contract/regression tests 对应改动链路。

#### 具体规则

##### 规则 1.1: 新增字段、状态、异常路径是否有对应测试

- 在 MR 中重点看: 与“风险覆盖”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“风险覆盖”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“风险覆盖”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 测试直接覆盖本次改动高风险点，能在回归时及时报警。
- 不良信号: 测试存在很多，但没有一条真正守住这次风险。
- 修复方向: 新增最小有力测试，优先覆盖高风险变更和历史易错点。

##### 规则 1.2: 跨文件语义变更是否有端到端或集成保护

- 在 MR 中重点看: 与“风险覆盖”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“风险覆盖”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“风险覆盖”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 测试直接覆盖本次改动高风险点，能在回归时及时报警。
- 不良信号: 测试存在很多，但没有一条真正守住这次风险。
- 修复方向: 新增最小有力测试，优先覆盖高风险变更和历史易错点。

##### 规则 1.3: 兼容性、旧数据、回放场景是否有回归测试

- 在 MR 中重点看: 与“风险覆盖”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“风险覆盖”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“风险覆盖”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 测试直接覆盖本次改动高风险点，能在回归时及时报警。
- 不良信号: 测试存在很多，但没有一条真正守住这次风险。
- 修复方向: 新增最小有力测试，优先覆盖高风险变更和历史易错点。

##### 规则 1.4: 异步、重试、幂等等高风险路径是否有验证

- 在 MR 中重点看: 与“风险覆盖”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“风险覆盖”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“风险覆盖”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 测试直接覆盖本次改动高风险点，能在回归时及时报警。
- 不良信号: 测试存在很多，但没有一条真正守住这次风险。
- 修复方向: 新增最小有力测试，优先覆盖高风险变更和历史易错点。

#### 这一维度的高风险反模式

- 主逻辑改了，测试一行没动且无其他保护。
- 只测 happy path，不测失败和边界路径。
- 新增兼容逻辑但没有旧数据/旧消息样本。
- 异步链路只测 producer 不测 consumer 结果。

#### 这一维度的审查追问

- 这次改动最可能回归的点是什么，是否有测试守住？
- 如果只有一条测试，该测哪条风险？
- 是否需要集成测试而不是单测？
- 旧数据/旧消息/旧缓存场景是否被覆盖？

### 维度 2: 断言质量

- 维度目标: 确认现有测试不是形式存在，而是真能发现错误。
- 重点搜索: assertion、mock verification、snapshot、exception assertion。

#### 具体规则

##### 规则 2.1: 断言是否覆盖关键输出和副作用

- 在 MR 中重点看: 与“断言质量”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“断言质量”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“断言质量”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 断言能准确抓住业务结果、关键副作用和异常边界。
- 不良信号: 测试绿了但线上仍可轻松回归同类问题。
- 修复方向: 改成语义化断言，减少空泛断言和纯交互式断言。

##### 规则 2.2: 是否只断言调用发生而不验证结果语义

- 在 MR 中重点看: 与“断言质量”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“断言质量”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“断言质量”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 断言能准确抓住业务结果、关键副作用和异常边界。
- 不良信号: 测试绿了但线上仍可轻松回归同类问题。
- 修复方向: 改成语义化断言，减少空泛断言和纯交互式断言。

##### 规则 2.3: snapshot/字符串断言是否过于脆弱或过于宽泛

- 在 MR 中重点看: 与“断言质量”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“断言质量”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“断言质量”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 断言能准确抓住业务结果、关键副作用和异常边界。
- 不良信号: 测试绿了但线上仍可轻松回归同类问题。
- 修复方向: 改成语义化断言，减少空泛断言和纯交互式断言。

##### 规则 2.4: 异常测试是否断言了错误类型和关键信息

- 在 MR 中重点看: 与“断言质量”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“断言质量”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“断言质量”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 断言能准确抓住业务结果、关键副作用和异常边界。
- 不良信号: 测试绿了但线上仍可轻松回归同类问题。
- 修复方向: 改成语义化断言，减少空泛断言和纯交互式断言。

#### 这一维度的高风险反模式

- 测试只 assertNotNull，根本测不出字段错。
- 全靠 mock verify 调用次数，不检查结果。
- 快照太大，变什么都看不出来。
- 异常测试只 catch Exception 就算通过。

#### 这一维度的审查追问

- 如果当前 bug 出现，这条测试真的会失败吗？
- 断言是在验证业务结果还是验证实现细节？
- 是否需要补更窄、更有语义的断言？
- 是否过度依赖 mock 导致真实问题被掩盖？

### 维度 3: 测试分层与夹具

- 维度目标: 确认测试层级合理，夹具和 mock 不会扭曲真实行为。
- 重点搜索: unit/integration slice、builders、fixtures、test containers、mock scope。

#### 具体规则

##### 规则 3.1: 该风险更适合单测、集成测还是契约测试

- 在 MR 中重点看: 与“测试分层与夹具”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“测试分层与夹具”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“测试分层与夹具”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 测试分层清晰，夹具表达意图，mock 只替换必要边界。
- 不良信号: 测试跑很快，但测到的是理想化世界。
- 修复方向: 选择正确测试层级，收敛 mock 并让夹具显式表达场景。

##### 规则 3.2: 夹具是否隐藏了真实默认值和边界条件

- 在 MR 中重点看: 与“测试分层与夹具”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“测试分层与夹具”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“测试分层与夹具”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 测试分层清晰，夹具表达意图，mock 只替换必要边界。
- 不良信号: 测试跑很快，但测到的是理想化世界。
- 修复方向: 选择正确测试层级，收敛 mock 并让夹具显式表达场景。

##### 规则 3.3: mock 是否把关键行为全部替掉，导致测试失真

- 在 MR 中重点看: 与“测试分层与夹具”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“测试分层与夹具”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“测试分层与夹具”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 测试分层清晰，夹具表达意图，mock 只替换必要边界。
- 不良信号: 测试跑很快，但测到的是理想化世界。
- 修复方向: 选择正确测试层级，收敛 mock 并让夹具显式表达场景。

##### 规则 3.4: 测试数据工厂是否让场景表达清晰可维护

- 在 MR 中重点看: 与“测试分层与夹具”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“测试分层与夹具”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“测试分层与夹具”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 测试分层清晰，夹具表达意图，mock 只替换必要边界。
- 不良信号: 测试跑很快，但测到的是理想化世界。
- 修复方向: 选择正确测试层级，收敛 mock 并让夹具显式表达场景。

#### 这一维度的高风险反模式

- 把所有依赖都 mock 掉，根本看不到真实集成问题。
- 测试数据工厂默认值太魔法，没人知道场景是什么。
- 同一个集成测试承担太多意图，失败难定位。
- 为了快，把真正关键的转换层全绕过。

#### 这一维度的审查追问

- 这个风险需要哪一层测试才能发现？
- 当前夹具是否掩盖了真实边界值？
- mock 掉这个依赖后，还能测出此次风险吗？
- 测试失败时能否快速定位原因？

### 维度 4: 可测性前提

- 维度目标: 确认代码结构允许后续补测试，而不是把验证成本推高到不可接受。
- 重点搜索: static dependency、time/random/env access、framework callback-heavy code。

#### 具体规则

##### 规则 4.1: 核心逻辑是否可直接调用

- 在 MR 中重点看: 与“可测性前提”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“可测性前提”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“可测性前提”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 补测试的成本合理，关键逻辑可独立验证。
- 不良信号: 每条测试都像搭脚手架，最终没人愿意补。
- 修复方向: 先做小步可测性重构，再补高价值回归测试。

##### 规则 4.2: 时间、随机性、环境依赖是否可控

- 在 MR 中重点看: 与“可测性前提”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“可测性前提”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“可测性前提”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 补测试的成本合理，关键逻辑可独立验证。
- 不良信号: 每条测试都像搭脚手架，最终没人愿意补。
- 修复方向: 先做小步可测性重构，再补高价值回归测试。

##### 规则 4.3: 副作用是否可在测试中观察到

- 在 MR 中重点看: 与“可测性前提”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“可测性前提”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“可测性前提”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 补测试的成本合理，关键逻辑可独立验证。
- 不良信号: 每条测试都像搭脚手架，最终没人愿意补。
- 修复方向: 先做小步可测性重构，再补高价值回归测试。

##### 规则 4.4: 构造测试输入是否需要大量无关步骤

- 在 MR 中重点看: 与“可测性前提”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“可测性前提”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“可测性前提”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 补测试的成本合理，关键逻辑可独立验证。
- 不良信号: 每条测试都像搭脚手架，最终没人愿意补。
- 修复方向: 先做小步可测性重构，再补高价值回归测试。

#### 这一维度的高风险反模式

- 逻辑散在注解回调和 static 工具类里。
- 系统时钟、随机数和线程上下文直接读写。
- 副作用没有任何可观察输出或可替换接口。
- 一个简单测试要准备一屏幕对象。

#### 这一维度的审查追问

- 如果现在补一条回归测试，最难的点是什么？
- 能否把时间、随机性、IO 提升成可注入依赖？
- 是否需要先做小重构再补测试？
- 测试数据是否过于依赖隐式默认值？

### 维度 5: 回归与发布验证

- 维度目标: 确认测试之外仍有必要的发布前验证与回归策略。
- 重点搜索: smoke、migration validation、replay、contract check、manual verification notes。

#### 具体规则

##### 规则 5.1: 本次改动是否需要额外 smoke 或灰度验证

- 在 MR 中重点看: 与“回归与发布验证”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“回归与发布验证”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“回归与发布验证”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 自动测试与必要的人工验证形成闭环。
- 不良信号: 发布靠感觉，不靠验证方案。
- 修复方向: 补最小 smoke、记录人工验证步骤，并把高频问题沉淀成回归测试。

##### 规则 5.2: 迁移、回放、缓存、消息等是否需要专门验证步骤

- 在 MR 中重点看: 与“回归与发布验证”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“回归与发布验证”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“回归与发布验证”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 自动测试与必要的人工验证形成闭环。
- 不良信号: 发布靠感觉，不靠验证方案。
- 修复方向: 补最小 smoke、记录人工验证步骤，并把高频问题沉淀成回归测试。

##### 规则 5.3: 人工验证步骤是否清晰可复现

- 在 MR 中重点看: 与“回归与发布验证”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“回归与发布验证”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“回归与发布验证”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 自动测试与必要的人工验证形成闭环。
- 不良信号: 发布靠感觉，不靠验证方案。
- 修复方向: 补最小 smoke、记录人工验证步骤，并把高频问题沉淀成回归测试。

##### 规则 5.4: 是否有值得沉淀成回归测试的历史问题模式

- 在 MR 中重点看: 与“回归与发布验证”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“回归与发布验证”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“回归与发布验证”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 自动测试与必要的人工验证形成闭环。
- 不良信号: 发布靠感觉，不靠验证方案。
- 修复方向: 补最小 smoke、记录人工验证步骤，并把高频问题沉淀成回归测试。

#### 这一维度的高风险反模式

- 全靠人工点点看，但没有任何操作步骤。
- 发布验证只看服务能启动，不看关键链路。
- 历史上踩过的坑这次又没守住。
- 需要人工验证的地方没有记录如何验证。

#### 这一维度的审查追问

- 上线前至少该跑哪条 smoke？
- 哪些验证很适合下一轮沉淀成自动测试？
- 这次是否涉及 migration、回放或异步链路验证？
- 人工验证步骤是否足够具体？

## 常见误报与越界提醒

- 不要把所有未改测试的改动都判成问题，先看风险和已有保护。
- 不要把覆盖率数字当充分证据，关键是是否守住这次改动的风险。
- 不要代替正确性专家定义业务结论，只评价验证是否足够。
- 不要因为使用 mock 就一概否定，重点看 mock 是否遮蔽关键风险。

## Java/Spring 场景化检查清单

- 检查 JUnit 5、Spring Boot Test、Testcontainers、MockMvc/WebTestClient 等测试层级选择。
- 检查断言是否覆盖字段语义、异常边界和副作用。
- 检查 builder/fixture/factory 是否表达真实场景。
- 检查时间、随机性、环境和外部依赖是否可控。
- 检查 migration、消息、缓存和回放是否有发布前验证。

## 典型评审场景

### 场景 1: DTO 新增字段但测试只断言对象非空

- 典型改动: MR 新增输出字段，测试没有改或只做弱断言。
- 第一反应不要做什么: 不要只说缺测试，要指出当前断言守不住什么。
- 应该先去看的代码: 看断言内容、transformer 测试和接口测试。
- 结论成立的关键证据: 说明字段为空或错误时测试依然会绿。
- 常见错误结论: “有测试就够了”。
- 更好的修复建议写法: 建议补字段级断言或契约测试。

### 场景 2: 异常路径未保护

- 典型改动: MR 新增 fallback、重试或异常分支。
- 第一反应不要做什么: 不要只补 happy path 测试。
- 应该先去看的代码: 看异常类型、重试次数和降级结果。
- 结论成立的关键证据: 指出失败路径当前完全无测试覆盖。
- 常见错误结论: “异常很少发生，不用测”。
- 更好的修复建议写法: 建议补失败分支和边界条件测试。

### 场景 3: 迁移需要验证但没有 smoke

- 典型改动: MR 改了 schema 或消息/缓存契约。
- 第一反应不要做什么: 不要只停留在单测，关注发布验证。
- 应该先去看的代码: 看 migration、旧数据、回放和灰度步骤。
- 结论成立的关键证据: 指出自动测试之外仍有关键风险未覆盖。
- 常见错误结论: “单测都过了，可以直接上”。
- 更好的修复建议写法: 建议补 smoke、迁移验证或回放验证步骤。

### 场景 4: mock 遮蔽真实问题

- 典型改动: MR 相关测试把关键转换层或仓储行为全 mock 掉。
- 第一反应不要做什么: 不要一概反 mock，要指出当前风险被遮蔽了。
- 应该先去看的代码: 看 mock 范围、真实依赖替换和断言对象。
- 结论成立的关键证据: 说明真实集成问题在当前测试中完全不可能暴露。
- 常见错误结论: “测试很多，所以有保障”。
- 更好的修复建议写法: 建议至少补一条更接近真实链路的集成测试。

### 场景 5: 回归 bug 没沉淀

- 典型改动: MR 修复了已知线上问题但没补回归测试。
- 第一反应不要做什么: 不要只要求补测试，要说明这是高价值回归保护。
- 应该先去看的代码: 看 bug 触发条件、最小复现场景和现有测试模板。
- 结论成立的关键证据: 指出同类问题未来仍可轻易复发。
- 常见错误结论: “修好了就行，不必留测试”。
- 更好的修复建议写法: 建议把最小复现场景沉淀成回归用例。

## 结论输出格式

- 命中的规范条款: 只列本专家真正命中的维度与规则编号。
- 问题定性: 在 `direct_defect / risk_hypothesis / test_gap / design_concern` 中选择一种，不要混写。
- 代码定位: 至少给出文件、方法或类名，以及对应的 diff hunk 或目标分支上下文位置。
- 证据说明: 先写 MR 片段证据，再写源码仓补充证据，缺一不可时要降级。
- 影响说明: 说明问题会影响哪类请求、哪条链路、哪种数据或哪类调用。
- 修复建议: 说明具体改法、推荐落点、需要一起修改的关联代码。
- 验证建议: 指出需要补的测试、脚本、回放或人工核对步骤。
- 移交建议: 若问题跨界，明确应该拉哪个专家继续看。

## 跨文件核对清单

- 当前 MR 修改的主文件是否只是入口，真正语义是否落在其他类、配置或转换层？
- 当前字段、状态、枚举、事件名是否在目标分支还有其他读写路径？
- 当前改动是否同步更新了异步消费者、批任务、报表、缓存或投影？
- 当前改动是否引入了新的前置条件、默认值或失败路径？
- 当前改动是否要求调用方、下游或历史数据同步迁移？
- 当前改动是否影响日志、审计、监控、告警或回放工具对链路的理解？
- 当前改动是否会在重试、并发、回滚、补偿、回放场景下产生不同结果？
- 当前改动是否只是把问题从主链路挪到其他任务、事件或缓存链路里？
- 当前改动若上线一半，系统是否仍然保持兼容和可恢复？
- 当前改动若被回滚，是否存在残留状态、脏数据或契约不兼容？

## 修复建议写法

- 先写“要改什么”，再写“为什么要这样改”，最后写“如何验证改对了”。
- 修复建议必须指出推荐落点，例如 aggregate、service、repository、migration、consumer、test。
- 若需要联动修改多个文件，要列出主改动点和至少一个关键联动点。
- 若问题涉及兼容性，要明确灰度、补数、回滚或双写双读策略。
- 若问题涉及时间、金额、幂等、权限、缓存、消息等高风险语义，要指出具体样例。
- 不要只写“建议重构”“建议优化”“建议补校验”这类无落点的空话。
- 建议代码修改方案应优先贴近当前项目技术栈和已存在实现，而不是重新发明框架。
- 若证据不足，修复建议应写成“补验证+补保护”，而不是强行给出大改方案。

## 快速口头判断禁令

- 禁止只因为看见 import、注解、字段名、目录名就直接下结论。
- 禁止只读单个 diff hunk 就宣称已经理解整条业务链路。
- 禁止把框架偏好、个人习惯或历史写法直接当成缺陷证据。
- 禁止在没有目标分支源码上下文时给高置信结论。
- 禁止把属于其他专家的风险强行写成自己的正式问题。
- 禁止只给“建议优化”却不写具体落点和验证方式。
- 禁止把旧代码已有问题全部记到本次 MR 头上，除非 MR 明确放大了风险。
- 禁止把偶发猜测写成必现缺陷，必须说明触发条件。
- 禁止把测试缺口直接等同于线上缺陷，除非已经有直接业务错误证据。
- 禁止在缺少兼容性证据时随意建议大规模重构或重写。
- 禁止忽略异步链路、回放、补偿、批任务和报表这类非主链路使用点。
- 禁止在修复建议里绕过现有架构和领域边界重新发明一套实现。

## 跨专家协作提示

- 当问题同时涉及两个以上领域时，先给出本专家职责内的主结论，再明确建议转交对象。
- 若某条问题的直接根因不在本专家边界内，不要抢结论，只能给出“我观察到的上游/下游信号”。
- 与架构专家协作时，重点提供本领域的直接证据，不要代替其判断总体边界。
- 与正确性专家协作时，重点提供技术语义如何放大业务错误，而不是重写业务定义。
- 与数据库专家协作时，重点提供缓存、消息、事务、契约如何影响数据库链路的补充证据。
- 与安全专家协作时，重点提供实际攻击面或数据边界线索，而不是含糊写成“有风险”。
- 与测试专家协作时，重点指出最需要保护的失败路径和最小可复现样例。
- 若问题已足以由本专家单独闭环，不要为了显得全面而硬拉其他专家。
- 若只是怀疑别的领域可能也有问题，要写成“建议进一步检查”，不要直接替他人定性。
- 在最终结论里，优先保留本专家最强的一到两条问题，而不是平铺大量弱结论。

## 证据引用模板

- MR 证据模板: `文件 + hunk + 当前改动前后差异 + 触发条件`。
- 源码仓证据模板: `目标分支文件 + 调用方/被调用方 + 现有实现假设`。
- 风险说明模板: `在什么输入/时序/负载/权限前提下会出错`。
- 修复建议模板: `修改落点 + 关键改法 + 联动文件 + 验证步骤`。
- 若是跨文件问题，必须至少列出“主修改点”和“至少一个受影响上下游文件”。
- 若是兼容性问题，必须列出“旧数据/旧缓存/旧消息/旧调用方”中至少一个实际受影响对象。
- 若是并发问题，必须列出“竞争窗口”或“顺序假设”。
- 若是权限问题，必须列出“攻击者或越权主体”与“被访问对象”。

## 结论自检

- 如果把专家名称遮住，这条结论还能看出明确的职责边界吗？
- 如果删掉源码仓上下文，这条结论是否就会失去关键证据？若会，说明引用方式是对的。
- 如果把修复建议交给开发同学，他是否能直接知道下一步要改哪里、补什么验证？

## 合并前最终检查

- 是否明确指出了缺少哪类验证以及它守不住哪类风险？
- 是否避免把覆盖率或测试数量当成唯一标准？
- 是否给出了可执行的测试或 smoke 补充建议？
- 是否兼顾了自动测试与发布前验证？

## 术语表

- 回归保护: 为防止同类问题再次出现而建立的测试或验证机制。
- 弱断言: 即使结果错误，测试也大概率不会失败的断言。
- 测试夹具: 用来快速构造场景的数据、对象或环境准备代码。
- 可测性: 代码被可靠验证所需要付出的结构成本。
