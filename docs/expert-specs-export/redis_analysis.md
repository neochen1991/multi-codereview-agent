# Redis 分析专家代码检视规范

## 文档定位

- 专家 ID: `redis_analysis`
- 专家角色: 关注缓存、TTL、原子性、一致性和内存风险的 Redis 专家
- 核心审视目标: 识别 key 设计、缓存一致性、过期策略和原子操作层面的 Redis 风险。
- 使用方式: 审视前必须同时加载 MR/PR diff、目标分支源码上下文、专家绑定 Markdown 文档与本规范。
- 输出原则: 只输出本专家职责边界内的结论；发现跨边界风险时必须显式建议转交其他专家。

## 权威参考来源

- [Redis Documentation](https://redis.io/docs/latest/)
- [Spring Data Redis Reference](https://docs.spring.io/spring-data/redis/reference/)
- [Redisson Reference Guide](https://redisson.pro/docs/)

## 职责边界

### 本专家必须负责

- key 命名、key 维度、TTL 语义、预热和失效策略。
- 缓存一致性、双写/删缓存时机、回源保护和击穿/雪崩/穿透。
- Lua、事务、setnx、分布式锁和原子操作语义。
- 内存占用、热点 key、序列化格式和大 value 风险。

### 本专家不负责

- 不负责数据库 schema 和索引，这属于数据库专家。
- 不负责业务语义正确性，这属于正确性专家。
- 不负责 MQ 语义，这属于 MQ 专家。
- 不负责总体性能策略，这属于性能专家。

### 遇到这些信号时必须移交

- 看到数据库回源和查询计划问题时移交数据库专家。
- 看到消息重试和幂等等消费语义时移交 MQ 专家。
- 看到整体超时重试风暴时移交性能专家。
- 看到业务字段含义和默认值错误时移交正确性专家。

## 固定审视流程

1. 先读 MR/PR 的目标 hunk，定位本次改动真正改了什么。
2. 再去目标分支源码仓检索同名方法、同名类型、调用方和被调用方。
3. 按本规范的维度逐条核对，只保留本专家职责边界内的直接证据。
4. 对证据不足的问题降级为待验证风险，不能因为经验感觉直接定性。
5. 修复建议必须说清楚怎么改、在哪改、哪些测试或验证要补齐。

## 源码仓检索策略

### 首选检索入口

- 搜索 cache、redis、ttl、expire、setIfAbsent、lua、lock、warmup。
- 搜索 key 拼接点、序列化器、回源逻辑和缓存刷新时机。
- 搜索删除缓存、更新缓存、双写和回放任务对缓存的影响。
- 搜索热点接口、批量任务和定时任务是否共用同一 key 空间。

### 证据闭环要求

- 必须说明 key、TTL 或更新时机会怎样导致脏数据或击穿。
- 原子性问题必须指出多步操作之间的竞态窗口。
- 不要只说“缓存可能不一致”，要说明在哪种读写顺序下会错。
- 内存和热点问题要指出 key 规模、value 体积或访问模式。

## 详细审视维度

### 维度 1: key 设计与 TTL

- 维度目标: 确认 key 维度和过期语义与业务场景匹配。
- 重点搜索: key builder、prefix、tenant/user dimension、ttl config。

#### 具体规则

##### 规则 1.1: key 是否包含必要业务维度和租户边界

- 在 MR 中重点看: 与“key 设计与 TTL”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“key 设计与 TTL”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“key 设计与 TTL”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: key 维度完整、TTL 明确、命名稳定可维护。
- 不良信号: 先缓存再说，key 和 TTL 都靠感觉。
- 修复方向: 统一 key 规范，按业务语义设置 TTL 并显式版本化。

##### 规则 1.2: TTL 是否与业务新鲜度和失效语义匹配

- 在 MR 中重点看: 与“key 设计与 TTL”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“key 设计与 TTL”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“key 设计与 TTL”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: key 维度完整、TTL 明确、命名稳定可维护。
- 不良信号: 先缓存再说，key 和 TTL 都靠感觉。
- 修复方向: 统一 key 规范，按业务语义设置 TTL 并显式版本化。

##### 规则 1.3: 无 TTL 或 TTL 过长是否会造成长期脏数据

- 在 MR 中重点看: 与“key 设计与 TTL”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“key 设计与 TTL”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“key 设计与 TTL”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: key 维度完整、TTL 明确、命名稳定可维护。
- 不良信号: 先缓存再说，key 和 TTL 都靠感觉。
- 修复方向: 统一 key 规范，按业务语义设置 TTL 并显式版本化。

##### 规则 1.4: 过短 TTL 是否会引发频繁回源和抖动

- 在 MR 中重点看: 与“key 设计与 TTL”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“key 设计与 TTL”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“key 设计与 TTL”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: key 维度完整、TTL 明确、命名稳定可维护。
- 不良信号: 先缓存再说，key 和 TTL 都靠感觉。
- 修复方向: 统一 key 规范，按业务语义设置 TTL 并显式版本化。

#### 这一维度的高风险反模式

- 所有租户共用同一个 key 前缀而无边界。
- 关键缓存永不过期，但又没有明确刷新机制。
- TTL 和业务有效期完全不匹配。
- 同一类数据不同地方用不同 key 规范。

#### 这一维度的审查追问

- 这个 key 是否区分租户、用户、版本和场景？
- TTL 到期后读者看到什么语义？
- 业务数据更新频率和 TTL 是否匹配？
- 这类 key 是否有统一命名与版本策略？

### 维度 2: 缓存一致性

- 维度目标: 确认写数据库、删缓存、写缓存和回源顺序不会制造长时间脏数据。
- 重点搜索: write-through、cache aside、delete cache、refresh、rebuild。

#### 具体规则

##### 规则 2.1: 写后删缓存还是写后更新缓存，顺序是否安全

- 在 MR 中重点看: 与“缓存一致性”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“缓存一致性”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“缓存一致性”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 缓存更新时机清晰，并发场景下旧值不会轻易回填。
- 不良信号: 缓存只是最佳努力，脏了多久没人知道。
- 修复方向: 明确 cache-aside 顺序，补版本比较、singleflight 和失败补偿。

##### 规则 2.2: 并发读写时是否会回填旧值

- 在 MR 中重点看: 与“缓存一致性”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“缓存一致性”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“缓存一致性”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 缓存更新时机清晰，并发场景下旧值不会轻易回填。
- 不良信号: 缓存只是最佳努力，脏了多久没人知道。
- 修复方向: 明确 cache-aside 顺序，补版本比较、singleflight 和失败补偿。

##### 规则 2.3: 异步更新缓存是否可能覆盖新值

- 在 MR 中重点看: 与“缓存一致性”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“缓存一致性”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“缓存一致性”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 缓存更新时机清晰，并发场景下旧值不会轻易回填。
- 不良信号: 缓存只是最佳努力，脏了多久没人知道。
- 修复方向: 明确 cache-aside 顺序，补版本比较、singleflight 和失败补偿。

##### 规则 2.4: 回源失败时是否会留下脏缓存或空缓存

- 在 MR 中重点看: 与“缓存一致性”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“缓存一致性”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“缓存一致性”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 缓存更新时机清晰，并发场景下旧值不会轻易回填。
- 不良信号: 缓存只是最佳努力，脏了多久没人知道。
- 修复方向: 明确 cache-aside 顺序，补版本比较、singleflight 和失败补偿。

#### 这一维度的高风险反模式

- 先删缓存再写库，窗口里读请求回填旧值。
- 异步重建把旧数据又写回缓存。
- 缓存 miss 后所有请求一起回源。
- 删除失败但系统无任何补偿或告警。

#### 这一维度的审查追问

- 数据库写成功到缓存更新之间有多大窗口？
- 并发读写时会不会把旧值重新塞回缓存？
- 异步刷新是否带版本或时间比较？
- 删缓存失败后系统如何补偿？

### 维度 3: 原子性与锁

- 维度目标: 确认多步操作不会因竞态导致重复写、丢失更新或假锁。
- 重点搜索: setnx、lua、multi/exec、redisson lock、unlock pattern。

#### 具体规则

##### 规则 3.1: 锁的获取、续期、释放是否成对且可追踪

- 在 MR 中重点看: 与“原子性与锁”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“原子性与锁”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“原子性与锁”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 原子操作明确，锁语义可靠，失败策略可控。
- 不良信号: 看起来有锁，实际上 owner、lease 和释放都不安全。
- 修复方向: 用 Lua/可靠锁封装原子操作，并明确 owner、lease 和失败退让。

##### 规则 3.2: 多步写缓存是否需要 Lua 或事务保证原子性

- 在 MR 中重点看: 与“原子性与锁”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“原子性与锁”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“原子性与锁”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 原子操作明确，锁语义可靠，失败策略可控。
- 不良信号: 看起来有锁，实际上 owner、lease 和释放都不安全。
- 修复方向: 用 Lua/可靠锁封装原子操作，并明确 owner、lease 和失败退让。

##### 规则 3.3: setnx/expire 是否分步造成死锁窗口

- 在 MR 中重点看: 与“原子性与锁”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“原子性与锁”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“原子性与锁”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 原子操作明确，锁语义可靠，失败策略可控。
- 不良信号: 看起来有锁，实际上 owner、lease 和释放都不安全。
- 修复方向: 用 Lua/可靠锁封装原子操作，并明确 owner、lease 和失败退让。

##### 规则 3.4: 锁失败、超时和重试是否会制造惊群

- 在 MR 中重点看: 与“原子性与锁”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“原子性与锁”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“原子性与锁”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 原子操作明确，锁语义可靠，失败策略可控。
- 不良信号: 看起来有锁，实际上 owner、lease 和释放都不安全。
- 修复方向: 用 Lua/可靠锁封装原子操作，并明确 owner、lease 和失败退让。

#### 这一维度的高风险反模式

- setnx 成功后进程崩溃，expire 还没设置。
- unlock 不校验 owner，误删别人的锁。
- 多 key 更新分步执行，中间读者看到半成品。
- 锁失败后无等待策略，所有请求疯狂重试。

#### 这一维度的审查追问

- 这个临界区真的需要锁，还是需要幂等？
- 锁的 owner 和 lease 是否可靠？
- 多步操作中间态是否可能被其他线程看到？
- 锁获取失败后系统如何退让？

### 维度 4: 热点与内存

- 维度目标: 确认 Redis 不会因热点 key、大 value 或无限增长而失控。
- 重点搜索: large value、hash/list/set growth、scan、hot key、serialization size。

#### 具体规则

##### 规则 4.1: 是否存在高频访问单一 key

- 在 MR 中重点看: 与“热点与内存”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“热点与内存”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“热点与内存”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: key 体积可控，热点有保护，集合增长有上限。
- 不良信号: Redis 被当对象仓库使用，越用越胖。
- 修复方向: 拆 key、裁剪集合、规避在线 scan，并为热点做分流或保护。

##### 规则 4.2: value 体积是否会导致网络和反序列化成本过大

- 在 MR 中重点看: 与“热点与内存”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“热点与内存”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“热点与内存”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: key 体积可控，热点有保护，集合增长有上限。
- 不良信号: Redis 被当对象仓库使用，越用越胖。
- 修复方向: 拆 key、裁剪集合、规避在线 scan，并为热点做分流或保护。

##### 规则 4.3: 集合类 key 是否无限增长

- 在 MR 中重点看: 与“热点与内存”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“热点与内存”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“热点与内存”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: key 体积可控，热点有保护，集合增长有上限。
- 不良信号: Redis 被当对象仓库使用，越用越胖。
- 修复方向: 拆 key、裁剪集合、规避在线 scan，并为热点做分流或保护。

##### 规则 4.4: scan/遍历类操作是否落在在线链路

- 在 MR 中重点看: 与“热点与内存”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“热点与内存”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“热点与内存”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: key 体积可控，热点有保护，集合增长有上限。
- 不良信号: Redis 被当对象仓库使用，越用越胖。
- 修复方向: 拆 key、裁剪集合、规避在线 scan，并为热点做分流或保护。

#### 这一维度的高风险反模式

- 一个 key 挂整个列表或整个租户对象。
- 在线请求里 scan 全 key 空间。
- 集合 key 从不清理，越积越大。
- 热点 key 所有流量都命中单节点。

#### 这一维度的审查追问

- 这个 key 的体积和访问频率有多大？
- 是否应该拆分大 value 或分页缓存？
- 集合是否有清理和裁剪策略？
- 热 key 是否需要本地缓存、分片或复制？

### 维度 5: 序列化与兼容

- 维度目标: 确认缓存对象结构变化不会让旧缓存失效或读出错误语义。
- 重点搜索: serializer、json schema、class name、version field、fallback parse。

#### 具体规则

##### 规则 5.1: 对象字段变化是否兼容旧缓存内容

- 在 MR 中重点看: 与“序列化与兼容”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“序列化与兼容”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“序列化与兼容”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 缓存格式有兼容策略，发布期间多版本可共存。
- 不良信号: 只要代码类结构一改，缓存全失效或全报错。
- 修复方向: 引入版本字段、弱绑定序列化或安全回退读取策略。

##### 规则 5.2: 序列化格式变化是否会让旧节点读失败

- 在 MR 中重点看: 与“序列化与兼容”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“序列化与兼容”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“序列化与兼容”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 缓存格式有兼容策略，发布期间多版本可共存。
- 不良信号: 只要代码类结构一改，缓存全失效或全报错。
- 修复方向: 引入版本字段、弱绑定序列化或安全回退读取策略。

##### 规则 5.3: class/version 绑定是否导致发布期间不兼容

- 在 MR 中重点看: 与“序列化与兼容”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“序列化与兼容”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“序列化与兼容”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 缓存格式有兼容策略，发布期间多版本可共存。
- 不良信号: 只要代码类结构一改，缓存全失效或全报错。
- 修复方向: 引入版本字段、弱绑定序列化或安全回退读取策略。

##### 规则 5.4: 缓存回填和读取是否对未知字段足够稳健

- 在 MR 中重点看: 与“序列化与兼容”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“序列化与兼容”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“序列化与兼容”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 缓存格式有兼容策略，发布期间多版本可共存。
- 不良信号: 只要代码类结构一改，缓存全失效或全报错。
- 修复方向: 引入版本字段、弱绑定序列化或安全回退读取策略。

#### 这一维度的高风险反模式

- 类结构一变，旧缓存全无法反序列化。
- 序列化绑定完整类名，重构包名后全线失效。
- 读取失败后默默吞错并当空值处理。
- 多版本节点并行时缓存格式不兼容。

#### 这一维度的审查追问

- 旧缓存对象在新代码里还能读吗？
- 序列化格式是否与类名强绑定？
- 读失败后系统会如何退化？
- 灰度发布期间多版本读写是否兼容？

## 常见误报与越界提醒

- 不要把所有数据不一致都归因于 Redis，先确认主源和更新顺序。
- 不要代替性能专家评价整体吞吐，只看 Redis 自身语义和局部放大点。
- 不要把缺缓存一律看成问题，先判断是否值得缓存。
- 不要把锁问题误判成业务幂等等问题。

## Java/Spring 场景化检查清单

- 检查 Spring Data Redis/Redisson 的 key、TTL、序列化和锁使用。
- 检查 cache aside、删除缓存、预热和重建路径。
- 检查 Lua、setnx、事务和 unlock owner 语义。
- 检查热点 key、大 value、scan 操作和集合增长。
- 检查灰度发布期缓存对象兼容性。

## 典型评审场景

### 场景 1: 新增时间字段但缓存对象没跟上

- 典型改动: MR 修改 DTO/缓存对象字段结构。
- 第一反应不要做什么: 不要只看序列化成功，要看旧缓存兼容。
- 应该先去看的代码: 看 serializer、缓存回填和旧值读取逻辑。
- 结论成立的关键证据: 指出旧缓存会读失败或读出错误默认值的位置。
- 常见错误结论: “缓存过期后自然就好了”。
- 更好的修复建议写法: 建议补版本兼容或回退解析策略，并考虑灰度窗口。

### 场景 2: 写后删缓存顺序不安全

- 典型改动: MR 调整写数据库和删缓存顺序。
- 第一反应不要做什么: 不要只说会脏，要说明并发窗口。
- 应该先去看的代码: 查看读写并发路径和缓存回填逻辑。
- 结论成立的关键证据: 说明旧值如何被重新写回缓存。
- 常见错误结论: “删缓存总比更新缓存安全”。
- 更好的修复建议写法: 建议显式设计 cache aside 顺序和并发保护。

### 场景 3: 使用 setnx + expire 分步加锁

- 典型改动: MR 手写简单 Redis 锁。
- 第一反应不要做什么: 不要只说锁不好，要指出 owner/lease 风险。
- 应该先去看的代码: 看加锁、续期、释放和异常路径。
- 结论成立的关键证据: 说明进程崩溃或超时后会留下什么问题。
- 常见错误结论: “能锁住就行”。
- 更好的修复建议写法: 建议用 Lua 或成熟锁封装保证原子性和 owner 校验。

### 场景 4: 缓存 miss 惊群

- 典型改动: MR 修改热点 key 或预热逻辑。
- 第一反应不要做什么: 不要代替数据库专家评价回源 SQL，只看 Redis 保护缺失。
- 应该先去看的代码: 看 singleflight、锁、限流和预热策略。
- 结论成立的关键证据: 说明 miss 时会有多少请求同时回源。
- 常见错误结论: “命中率高就没问题”。
- 更好的修复建议写法: 建议加单飞、回源限流或主动预热。

### 场景 5: 大 value 挂在单 key

- 典型改动: MR 把更多字段直接塞进缓存对象。
- 第一反应不要做什么: 不要只说内存大，要看热点访问和序列化成本。
- 应该先去看的代码: 看 value 大小、频率和读取模式。
- 结论成立的关键证据: 指出网络、反序列化或节点热点的具体影响。
- 常见错误结论: “Redis 内存够大就行”。
- 更好的修复建议写法: 建议拆 key、分页缓存或按访问模式拆分对象。

## 结论输出格式

- 命中的规范条款: 只列本专家真正命中的维度与规则编号。
- 问题定性: 在 `direct_defect / risk_hypothesis / test_gap / design_concern` 中选择一种，不要混写。
- 代码定位: 至少给出文件、方法或类名，以及对应的 diff hunk 或目标分支上下文位置。
- 证据说明: 先写 MR 片段证据，再写源码仓补充证据，缺一不可时要降级。
- 影响说明: 说明问题会影响哪类请求、哪条链路、哪种数据或哪类调用。
- 修复建议: 说明具体改法、推荐落点、需要一起修改的关联代码。
- 验证建议: 指出需要补的测试、脚本、回放或人工核对步骤。
- 移交建议: 若问题跨界，明确应该拉哪个专家继续看。

## 跨文件核对清单

- 当前 MR 修改的主文件是否只是入口，真正语义是否落在其他类、配置或转换层？
- 当前字段、状态、枚举、事件名是否在目标分支还有其他读写路径？
- 当前改动是否同步更新了异步消费者、批任务、报表、缓存或投影？
- 当前改动是否引入了新的前置条件、默认值或失败路径？
- 当前改动是否要求调用方、下游或历史数据同步迁移？
- 当前改动是否影响日志、审计、监控、告警或回放工具对链路的理解？
- 当前改动是否会在重试、并发、回滚、补偿、回放场景下产生不同结果？
- 当前改动是否只是把问题从主链路挪到其他任务、事件或缓存链路里？
- 当前改动若上线一半，系统是否仍然保持兼容和可恢复？
- 当前改动若被回滚，是否存在残留状态、脏数据或契约不兼容？

## 修复建议写法

- 先写“要改什么”，再写“为什么要这样改”，最后写“如何验证改对了”。
- 修复建议必须指出推荐落点，例如 aggregate、service、repository、migration、consumer、test。
- 若需要联动修改多个文件，要列出主改动点和至少一个关键联动点。
- 若问题涉及兼容性，要明确灰度、补数、回滚或双写双读策略。
- 若问题涉及时间、金额、幂等、权限、缓存、消息等高风险语义，要指出具体样例。
- 不要只写“建议重构”“建议优化”“建议补校验”这类无落点的空话。
- 建议代码修改方案应优先贴近当前项目技术栈和已存在实现，而不是重新发明框架。
- 若证据不足，修复建议应写成“补验证+补保护”，而不是强行给出大改方案。

## 快速口头判断禁令

- 禁止只因为看见 import、注解、字段名、目录名就直接下结论。
- 禁止只读单个 diff hunk 就宣称已经理解整条业务链路。
- 禁止把框架偏好、个人习惯或历史写法直接当成缺陷证据。
- 禁止在没有目标分支源码上下文时给高置信结论。
- 禁止把属于其他专家的风险强行写成自己的正式问题。
- 禁止只给“建议优化”却不写具体落点和验证方式。
- 禁止把旧代码已有问题全部记到本次 MR 头上，除非 MR 明确放大了风险。
- 禁止把偶发猜测写成必现缺陷，必须说明触发条件。
- 禁止把测试缺口直接等同于线上缺陷，除非已经有直接业务错误证据。
- 禁止在缺少兼容性证据时随意建议大规模重构或重写。
- 禁止忽略异步链路、回放、补偿、批任务和报表这类非主链路使用点。
- 禁止在修复建议里绕过现有架构和领域边界重新发明一套实现。

## 跨专家协作提示

- 当问题同时涉及两个以上领域时，先给出本专家职责内的主结论，再明确建议转交对象。
- 若某条问题的直接根因不在本专家边界内，不要抢结论，只能给出“我观察到的上游/下游信号”。
- 与架构专家协作时，重点提供本领域的直接证据，不要代替其判断总体边界。
- 与正确性专家协作时，重点提供技术语义如何放大业务错误，而不是重写业务定义。
- 与数据库专家协作时，重点提供缓存、消息、事务、契约如何影响数据库链路的补充证据。
- 与安全专家协作时，重点提供实际攻击面或数据边界线索，而不是含糊写成“有风险”。
- 与测试专家协作时，重点指出最需要保护的失败路径和最小可复现样例。
- 若问题已足以由本专家单独闭环，不要为了显得全面而硬拉其他专家。
- 若只是怀疑别的领域可能也有问题，要写成“建议进一步检查”，不要直接替他人定性。
- 在最终结论里，优先保留本专家最强的一到两条问题，而不是平铺大量弱结论。

## 证据引用模板

- MR 证据模板: `文件 + hunk + 当前改动前后差异 + 触发条件`。
- 源码仓证据模板: `目标分支文件 + 调用方/被调用方 + 现有实现假设`。
- 风险说明模板: `在什么输入/时序/负载/权限前提下会出错`。
- 修复建议模板: `修改落点 + 关键改法 + 联动文件 + 验证步骤`。
- 若是跨文件问题，必须至少列出“主修改点”和“至少一个受影响上下游文件”。
- 若是兼容性问题，必须列出“旧数据/旧缓存/旧消息/旧调用方”中至少一个实际受影响对象。
- 若是并发问题，必须列出“竞争窗口”或“顺序假设”。
- 若是权限问题，必须列出“攻击者或越权主体”与“被访问对象”。

## 结论自检

- 如果把专家名称遮住，这条结论还能看出明确的职责边界吗？
- 如果删掉源码仓上下文，这条结论是否就会失去关键证据？若会，说明引用方式是对的。
- 如果把修复建议交给开发同学，他是否能直接知道下一步要改哪里、补什么验证？

## 合并前最终检查

- 是否明确指出了 key、TTL、一致性或锁语义中的具体问题？
- 是否给出了并发窗口、兼容窗口或热点证据？
- 是否避免越界评价数据库和 MQ 语义？
- 是否给出了 Redis 侧可执行的修复建议？

## 术语表

- cache aside: 先读缓存，未命中回源，再回填缓存的常见模式。
- singleflight: 多个并发请求合并成一次回源。
- 热 key: 短时间内访问量极高的单个 key。
- 原子性: 一组操作要么全成功，要么对外不可见中间态。
