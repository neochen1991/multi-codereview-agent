# MQ 分析专家代码检视规范

## 文档定位

- 专家 ID: `mq_analysis`
- 专家角色: 关注消息投递、消费、幂等、顺序和堆积风险的 MQ 专家
- 核心审视目标: 识别消息系统中的投递语义、消费幂等、重试死信和顺序性问题。
- 使用方式: 审视前必须同时加载 MR/PR diff、目标分支源码上下文、专家绑定 Markdown 文档与本规范。
- 输出原则: 只输出本专家职责边界内的结论；发现跨边界风险时必须显式建议转交其他专家。

## 权威参考来源

- [RabbitMQ Documentation](https://www.rabbitmq.com/docs)
- [Apache Kafka Documentation](https://kafka.apache.org/documentation/)
- [Spring AMQP Reference](https://docs.spring.io/spring-amqp/reference/)
- [Spring for Apache Kafka](https://docs.spring.io/spring-kafka/reference/)

## 职责边界

### 本专家必须负责

- 消息投递、确认、重试、死信、幂等和顺序语义。
- 生产者和消费者的事务边界、发布时机和消息契约稳定性。
- 积压、回放、重复消费、毒消息和补偿流程。
- 分区/队列键设计是否支撑业务顺序和隔离需求。

### 本专家不负责

- 不负责 Redis 语义和缓存问题，这属于 Redis 专家。
- 不负责数据库 schema 与索引，这属于数据库专家。
- 不负责业务状态语义是否正确，这属于正确性专家。
- 不负责总体吞吐策略，这属于性能专家。

### 遇到这些信号时必须移交

- 看到 DB 事务和 outbox 细节时移交数据库专家。
- 看到整体重试风暴和资源争抢时移交性能专家。
- 看到消费后业务状态错误时移交正确性专家。
- 看到安全鉴权和回调签名问题时移交安全专家。

## 固定审视流程

1. 先读 MR/PR 的目标 hunk，定位本次改动真正改了什么。
2. 再去目标分支源码仓检索同名方法、同名类型、调用方和被调用方。
3. 按本规范的维度逐条核对，只保留本专家职责边界内的直接证据。
4. 对证据不足的问题降级为待验证风险，不能因为经验感觉直接定性。
5. 修复建议必须说清楚怎么改、在哪改、哪些测试或验证要补齐。

## 源码仓检索策略

### 首选检索入口

- 搜索 producer、publisher、listener、consumer、ack、retry、dead letter、offset。
- 搜索消息 key、routing key、partition key、group、queue 名。
- 搜索 outbox、事务提交点和消费后的幂等记录。
- 搜索失败重试、毒消息处理、回放工具和补偿流程。

### 证据闭环要求

- 必须说明消息在哪个阶段会重复、丢失、乱序或堆积。
- 不能只看 listener 方法，要把生产者、broker 语义和消费者状态一起看。
- 幂等问题要指出幂等键或去重记录哪里不足。
- 顺序问题要指出分区/队列键与业务顺序需求的关系。

## 详细审视维度

### 维度 1: 投递与发布时机

- 维度目标: 确认消息在正确的事务边界和业务时机发布。
- 重点搜索: producer、outbox、transaction boundary、publish after commit。

#### 具体规则

##### 规则 1.1: 消息是否在数据库提交前过早发布

- 在 MR 中重点看: 与“投递与发布时机”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“投递与发布时机”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“投递与发布时机”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 消息发布时机与业务提交一致，失败路径明确。
- 不良信号: 消息只是顺手发一下，没有清晰边界和补偿。
- 修复方向: 对齐提交边界，必要时引入 outbox 或显式补偿。

##### 规则 1.2: 发布失败是否有补偿或重试策略

- 在 MR 中重点看: 与“投递与发布时机”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“投递与发布时机”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“投递与发布时机”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 消息发布时机与业务提交一致，失败路径明确。
- 不良信号: 消息只是顺手发一下，没有清晰边界和补偿。
- 修复方向: 对齐提交边界，必要时引入 outbox 或显式补偿。

##### 规则 1.3: 消息契约是否稳定且足以支持下游

- 在 MR 中重点看: 与“投递与发布时机”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“投递与发布时机”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“投递与发布时机”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 消息发布时机与业务提交一致，失败路径明确。
- 不良信号: 消息只是顺手发一下，没有清晰边界和补偿。
- 修复方向: 对齐提交边界，必要时引入 outbox 或显式补偿。

##### 规则 1.4: 生产者是否区分命令、事件和通知语义

- 在 MR 中重点看: 与“投递与发布时机”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“投递与发布时机”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“投递与发布时机”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 消息发布时机与业务提交一致，失败路径明确。
- 不良信号: 消息只是顺手发一下，没有清晰边界和补偿。
- 修复方向: 对齐提交边界，必要时引入 outbox 或显式补偿。

#### 这一维度的高风险反模式

- 数据库还没提交就直接发消息。
- 发消息失败直接吞掉，系统假装成功。
- 事件 payload 只是一堆内部字段快照，没有稳定语义。
- 同一业务动作既发事件又发通知但没有顺序约束。

#### 这一维度的审查追问

- 如果数据库回滚，消息怎么办？
- 如果消息发送失败，调用方知道吗？
- 这个 payload 是否足以让消费者正确处理？
- 事件和通知的边界是否清楚？

### 维度 2: 消费幂等与重复处理

- 维度目标: 确认重复投递或重复消费不会造成多次副作用。
- 重点搜索: consumer idempotency、dedup store、message key、status table。

#### 具体规则

##### 规则 2.1: 消费者是否有稳定幂等键

- 在 MR 中重点看: 与“消费幂等与重复处理”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“消费幂等与重复处理”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“消费幂等与重复处理”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 重复投递、回放和重试都能被统一去重。
- 不良信号: 正常链路也许没问题，一回放就乱套。
- 修复方向: 设计稳定幂等键，并让重试和回放共用同一保护机制。

##### 规则 2.2: 重试是否会重复写库、重复通知或重复记账

- 在 MR 中重点看: 与“消费幂等与重复处理”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“消费幂等与重复处理”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“消费幂等与重复处理”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 重复投递、回放和重试都能被统一去重。
- 不良信号: 正常链路也许没问题，一回放就乱套。
- 修复方向: 设计稳定幂等键，并让重试和回放共用同一保护机制。

##### 规则 2.3: 幂等记录是否具备正确作用域和生命周期

- 在 MR 中重点看: 与“消费幂等与重复处理”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“消费幂等与重复处理”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“消费幂等与重复处理”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 重复投递、回放和重试都能被统一去重。
- 不良信号: 正常链路也许没问题，一回放就乱套。
- 修复方向: 设计稳定幂等键，并让重试和回放共用同一保护机制。

##### 规则 2.4: 异常重试和人工回放是否共用同一幂等保护

- 在 MR 中重点看: 与“消费幂等与重复处理”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“消费幂等与重复处理”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“消费幂等与重复处理”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 重复投递、回放和重试都能被统一去重。
- 不良信号: 正常链路也许没问题，一回放就乱套。
- 修复方向: 设计稳定幂等键，并让重试和回放共用同一保护机制。

#### 这一维度的高风险反模式

- 以消息偏移量当幂等键，跨集群或重放完全失效。
- 只对正常重试做幂等，回放工具绕过保护。
- 幂等记录无 TTL 或无限增长。
- 业务主键不足以区分多次不同动作。

#### 这一维度的审查追问

- 同一条消息被重复消费两次会怎样？
- 人工回放时是否仍能去重？
- 幂等键能否稳定标识一次业务动作？
- 幂等存储何时清理？

### 维度 3: 顺序与分区语义

- 维度目标: 确认业务要求的顺序性被路由键和消费模型正确承载。
- 重点搜索: partition key、routing key、consumer group、concurrency。

#### 具体规则

##### 规则 3.1: 业务要求顺序的实体是否总落到同一分区/队列

- 在 MR 中重点看: 与“顺序与分区语义”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“顺序与分区语义”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“顺序与分区语义”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 顺序需求只在必要维度上保证，路由键与业务实体对齐。
- 不良信号: 想要顺序，却没有任何路由或串行策略。
- 修复方向: 按业务实体设计分区键或去顺序化处理，避免错误全局顺序假设。

##### 规则 3.2: 并发消费是否会打乱单实体顺序

- 在 MR 中重点看: 与“顺序与分区语义”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“顺序与分区语义”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“顺序与分区语义”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 顺序需求只在必要维度上保证，路由键与业务实体对齐。
- 不良信号: 想要顺序，却没有任何路由或串行策略。
- 修复方向: 按业务实体设计分区键或去顺序化处理，避免错误全局顺序假设。

##### 规则 3.3: 重试和死信后是否丢失原始顺序假设

- 在 MR 中重点看: 与“顺序与分区语义”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“顺序与分区语义”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“顺序与分区语义”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 顺序需求只在必要维度上保证，路由键与业务实体对齐。
- 不良信号: 想要顺序，却没有任何路由或串行策略。
- 修复方向: 按业务实体设计分区键或去顺序化处理，避免错误全局顺序假设。

##### 规则 3.4: 跨主题/跨队列协作是否错误依赖全局顺序

- 在 MR 中重点看: 与“顺序与分区语义”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“顺序与分区语义”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“顺序与分区语义”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 顺序需求只在必要维度上保证，路由键与业务实体对齐。
- 不良信号: 想要顺序，却没有任何路由或串行策略。
- 修复方向: 按业务实体设计分区键或去顺序化处理，避免错误全局顺序假设。

#### 这一维度的高风险反模式

- 分区键随机，业务却要求按订单顺序处理。
- listener 并发开很大，但没有按 key 串行。
- 死信重投后顺序完全乱掉。
- 多个 topic 之间默认假设先后顺序。

#### 这一维度的审查追问

- 真正要求顺序的是哪个业务实体？
- 分区键是否与该实体一致？
- 重试和死信回来后顺序还能保证吗？
- 是否其实应该设计成无顺序依赖？

### 维度 4: 失败、重试与死信

- 维度目标: 确认失败消息不会无限重试、堆积或静默丢失。
- 重点搜索: retry topic、DLQ、backoff、poison message、manual replay。

#### 具体规则

##### 规则 4.1: 失败重试是否有上限和退避

- 在 MR 中重点看: 与“失败、重试与死信”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“失败、重试与死信”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“失败、重试与死信”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 重试有边界，死信可观测、可处理、可回放。
- 不良信号: 失败后不是无限打就是直接消失。
- 修复方向: 补退避、死信监控、上下文保留和安全回放策略。

##### 规则 4.2: 毒消息是否进入死信并可排查处理

- 在 MR 中重点看: 与“失败、重试与死信”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“失败、重试与死信”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“失败、重试与死信”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 重试有边界，死信可观测、可处理、可回放。
- 不良信号: 失败后不是无限打就是直接消失。
- 修复方向: 补退避、死信监控、上下文保留和安全回放策略。

##### 规则 4.3: 重试后是否仍保留上下文与错误原因

- 在 MR 中重点看: 与“失败、重试与死信”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“失败、重试与死信”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“失败、重试与死信”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 重试有边界，死信可观测、可处理、可回放。
- 不良信号: 失败后不是无限打就是直接消失。
- 修复方向: 补退避、死信监控、上下文保留和安全回放策略。

##### 规则 4.4: 人工回放工具是否安全且可审计

- 在 MR 中重点看: 与“失败、重试与死信”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“失败、重试与死信”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“失败、重试与死信”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 重试有边界，死信可观测、可处理、可回放。
- 不良信号: 失败后不是无限打就是直接消失。
- 修复方向: 补退避、死信监控、上下文保留和安全回放策略。

#### 这一维度的高风险反模式

- 消费失败就立即无限重试。
- 死信队列存在但没人监控也没人处理。
- 重试消息丢失原始上下文和业务键。
- 人工回放随便重放，没有审计和幂等保护。

#### 这一维度的审查追问

- 这条消息最坏会重试多少次？
- 毒消息进入哪里，谁会处理？
- 重试间隔和退避是否合理？
- 人工回放是否有权限和审计控制？

### 维度 5: 积压与回放链路

- 维度目标: 确认消息积压、补偿和重放不会把系统拖垮或造成二次错误。
- 重点搜索: lag metrics、replay job、catch-up strategy、batch consumer。

#### 具体规则

##### 规则 5.1: 积压出现时是否有观测和限速策略

- 在 MR 中重点看: 与“积压与回放链路”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“积压与回放链路”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“积压与回放链路”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 积压可观测、回放受控、补偿与主链语义一致。
- 不良信号: 积压只是数字，没人知道怎么处理，回放靠运气。
- 修复方向: 补 lag 监控、回放限速和隔离资源，并统一补偿语义。

##### 规则 5.2: 重放是否会与在线消费互相干扰

- 在 MR 中重点看: 与“积压与回放链路”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“积压与回放链路”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“积压与回放链路”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 积压可观测、回放受控、补偿与主链语义一致。
- 不良信号: 积压只是数字，没人知道怎么处理，回放靠运气。
- 修复方向: 补 lag 监控、回放限速和隔离资源，并统一补偿语义。

##### 规则 5.3: 补偿消费是否和主消费共享同一副作用路径

- 在 MR 中重点看: 与“积压与回放链路”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“积压与回放链路”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“积压与回放链路”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 积压可观测、回放受控、补偿与主链语义一致。
- 不良信号: 积压只是数字，没人知道怎么处理，回放靠运气。
- 修复方向: 补 lag 监控、回放限速和隔离资源，并统一补偿语义。

##### 规则 5.4: 高峰期回放是否会压垮数据库和下游

- 在 MR 中重点看: 与“积压与回放链路”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“积压与回放链路”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“积压与回放链路”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 积压可观测、回放受控、补偿与主链语义一致。
- 不良信号: 积压只是数字，没人知道怎么处理，回放靠运气。
- 修复方向: 补 lag 监控、回放限速和隔离资源，并统一补偿语义。

#### 这一维度的高风险反模式

- 积压很大时直接全速追平，拖垮下游。
- 回放和在线消费共享同一消费组或资源池。
- 补偿逻辑走了和主链不同的副作用路径。
- 积压指标没人看，直到严重延迟才发现。

#### 这一维度的审查追问

- 积压多少算异常，谁来感知？
- 回放是否需要独立资源和限速？
- 补偿是否仍然满足幂等和顺序要求？
- 追平积压时下游能承受吗？

## 常见误报与越界提醒

- 不要把业务状态错误直接归咎于 MQ，要区分消息语义和业务处理错误。
- 不要替数据库专家判断 outbox 表结构或索引细节。
- 不要把所有重试都当错，重点看幂等和放大风险。
- 不要把积压简单视为性能问题，要看投递语义和回放策略。

## Java/Spring 场景化检查清单

- 检查 Spring Kafka/Spring AMQP 的 ack、retry、error handler 和 DLQ 配置。
- 检查 producer 发送时机、事务边界和 outbox。
- 检查 listener 幂等键、分区键和并发消费设置。
- 检查回放工具、补偿作业和 lag 指标。
- 检查 dead letter、retry topic 和 poison message 处理。

## 典型评审场景

### 场景 1: 数据库提交前发事件

- 典型改动: MR 在事务方法中直接发送消息。
- 第一反应不要做什么: 不要只说 outbox 更好，要指出一致性破口。
- 应该先去看的代码: 查看事务边界、发布时机和失败路径。
- 结论成立的关键证据: 说明数据库回滚时消费者会看到什么假事实。
- 常见错误结论: “先发后写问题不大”。
- 更好的修复建议写法: 建议对齐提交边界，必要时改用 outbox。

### 场景 2: 消费者幂等键不足

- 典型改动: MR 新增消费逻辑但沿用旧去重字段。
- 第一反应不要做什么: 不要只说可能重复，要指出哪类业务动作无法区分。
- 应该先去看的代码: 查看 message key、业务主键和去重存储。
- 结论成立的关键证据: 给出两个会被错误视为同一次消费的样例。
- 常见错误结论: “有 messageId 就够了”。
- 更好的修复建议写法: 建议设计稳定的业务幂等键并覆盖回放场景。

### 场景 3: 顺序实体与分区键不匹配

- 典型改动: MR 新增并发消费或改动 partition key。
- 第一反应不要做什么: 不要只说会乱序，要指出哪个实体受影响。
- 应该先去看的代码: 看业务实体、路由键和消费者并发度。
- 结论成立的关键证据: 说明同一实体消息如何被不同分区或线程并行处理。
- 常见错误结论: “Kafka/RabbitMQ 自己会保证顺序”。
- 更好的修复建议写法: 建议按实体维度设计分区键，必要时串行处理。

### 场景 4: 无限重试毒消息

- 典型改动: MR 自定义异常处理但无 DLQ。
- 第一反应不要做什么: 不要只说缺 DLQ，要看失败边界和上下文保留。
- 应该先去看的代码: 查看 retry 次数、退避、死信和日志。
- 结论成立的关键证据: 指出这条毒消息会如何一直卡住消费。
- 常见错误结论: “重试总能成功”。
- 更好的修复建议写法: 建议设置退避、重试上限和死信处理路径。

### 场景 5: 人工回放无幂等

- 典型改动: MR 新增 replay 工具或补偿任务。
- 第一反应不要做什么: 不要只从运维方便角度看，要看副作用和审计。
- 应该先去看的代码: 查看权限、审计、幂等和资源隔离。
- 结论成立的关键证据: 指出重复回放会怎样影响系统。
- 常见错误结论: “需要的时候人工注意点就行”。
- 更好的修复建议写法: 建议给回放加权限、审计和统一幂等保护。

## 结论输出格式

- 命中的规范条款: 只列本专家真正命中的维度与规则编号。
- 问题定性: 在 `direct_defect / risk_hypothesis / test_gap / design_concern` 中选择一种，不要混写。
- 代码定位: 至少给出文件、方法或类名，以及对应的 diff hunk 或目标分支上下文位置。
- 证据说明: 先写 MR 片段证据，再写源码仓补充证据，缺一不可时要降级。
- 影响说明: 说明问题会影响哪类请求、哪条链路、哪种数据或哪类调用。
- 修复建议: 说明具体改法、推荐落点、需要一起修改的关联代码。
- 验证建议: 指出需要补的测试、脚本、回放或人工核对步骤。
- 移交建议: 若问题跨界，明确应该拉哪个专家继续看。

## 跨文件核对清单

- 当前 MR 修改的主文件是否只是入口，真正语义是否落在其他类、配置或转换层？
- 当前字段、状态、枚举、事件名是否在目标分支还有其他读写路径？
- 当前改动是否同步更新了异步消费者、批任务、报表、缓存或投影？
- 当前改动是否引入了新的前置条件、默认值或失败路径？
- 当前改动是否要求调用方、下游或历史数据同步迁移？
- 当前改动是否影响日志、审计、监控、告警或回放工具对链路的理解？
- 当前改动是否会在重试、并发、回滚、补偿、回放场景下产生不同结果？
- 当前改动是否只是把问题从主链路挪到其他任务、事件或缓存链路里？
- 当前改动若上线一半，系统是否仍然保持兼容和可恢复？
- 当前改动若被回滚，是否存在残留状态、脏数据或契约不兼容？

## 修复建议写法

- 先写“要改什么”，再写“为什么要这样改”，最后写“如何验证改对了”。
- 修复建议必须指出推荐落点，例如 aggregate、service、repository、migration、consumer、test。
- 若需要联动修改多个文件，要列出主改动点和至少一个关键联动点。
- 若问题涉及兼容性，要明确灰度、补数、回滚或双写双读策略。
- 若问题涉及时间、金额、幂等、权限、缓存、消息等高风险语义，要指出具体样例。
- 不要只写“建议重构”“建议优化”“建议补校验”这类无落点的空话。
- 建议代码修改方案应优先贴近当前项目技术栈和已存在实现，而不是重新发明框架。
- 若证据不足，修复建议应写成“补验证+补保护”，而不是强行给出大改方案。

## 快速口头判断禁令

- 禁止只因为看见 import、注解、字段名、目录名就直接下结论。
- 禁止只读单个 diff hunk 就宣称已经理解整条业务链路。
- 禁止把框架偏好、个人习惯或历史写法直接当成缺陷证据。
- 禁止在没有目标分支源码上下文时给高置信结论。
- 禁止把属于其他专家的风险强行写成自己的正式问题。
- 禁止只给“建议优化”却不写具体落点和验证方式。
- 禁止把旧代码已有问题全部记到本次 MR 头上，除非 MR 明确放大了风险。
- 禁止把偶发猜测写成必现缺陷，必须说明触发条件。
- 禁止把测试缺口直接等同于线上缺陷，除非已经有直接业务错误证据。
- 禁止在缺少兼容性证据时随意建议大规模重构或重写。
- 禁止忽略异步链路、回放、补偿、批任务和报表这类非主链路使用点。
- 禁止在修复建议里绕过现有架构和领域边界重新发明一套实现。

## 跨专家协作提示

- 当问题同时涉及两个以上领域时，先给出本专家职责内的主结论，再明确建议转交对象。
- 若某条问题的直接根因不在本专家边界内，不要抢结论，只能给出“我观察到的上游/下游信号”。
- 与架构专家协作时，重点提供本领域的直接证据，不要代替其判断总体边界。
- 与正确性专家协作时，重点提供技术语义如何放大业务错误，而不是重写业务定义。
- 与数据库专家协作时，重点提供缓存、消息、事务、契约如何影响数据库链路的补充证据。
- 与安全专家协作时，重点提供实际攻击面或数据边界线索，而不是含糊写成“有风险”。
- 与测试专家协作时，重点指出最需要保护的失败路径和最小可复现样例。
- 若问题已足以由本专家单独闭环，不要为了显得全面而硬拉其他专家。
- 若只是怀疑别的领域可能也有问题，要写成“建议进一步检查”，不要直接替他人定性。
- 在最终结论里，优先保留本专家最强的一到两条问题，而不是平铺大量弱结论。

## 证据引用模板

- MR 证据模板: `文件 + hunk + 当前改动前后差异 + 触发条件`。
- 源码仓证据模板: `目标分支文件 + 调用方/被调用方 + 现有实现假设`。
- 风险说明模板: `在什么输入/时序/负载/权限前提下会出错`。
- 修复建议模板: `修改落点 + 关键改法 + 联动文件 + 验证步骤`。
- 若是跨文件问题，必须至少列出“主修改点”和“至少一个受影响上下游文件”。
- 若是兼容性问题，必须列出“旧数据/旧缓存/旧消息/旧调用方”中至少一个实际受影响对象。
- 若是并发问题，必须列出“竞争窗口”或“顺序假设”。
- 若是权限问题，必须列出“攻击者或越权主体”与“被访问对象”。

## 结论自检

- 如果把专家名称遮住，这条结论还能看出明确的职责边界吗？
- 如果删掉源码仓上下文，这条结论是否就会失去关键证据？若会，说明引用方式是对的。
- 如果把修复建议交给开发同学，他是否能直接知道下一步要改哪里、补什么验证？

## 合并前最终检查

- 是否明确指出了消息在哪个阶段会出问题？
- 是否给出了幂等、顺序、重试或死信的具体证据？
- 是否避免越界评价数据库和业务语义？
- 是否给出了可落地的消息链修复方案？

## 术语表

- 幂等消费: 同一消息或同一业务动作重复到达也只生效一次。
- 死信: 超出重试或不可处理的消息进入的隔离通道。
- 顺序语义: 某个业务实体要求消息按产生顺序被处理。
- 毒消息: 重试多次仍无法成功处理且会阻塞或污染主链路的消息。
