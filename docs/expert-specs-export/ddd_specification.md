# DDD 规范专家代码检视规范

## 文档定位

- 专家 ID: `ddd_specification`
- 专家角色: 面向聚合、上下文边界与领域语言的 DDD 审视专家
- 核心审视目标: 识别聚合边界失真、领域服务滥用、仓储抽象失真和上下文耦合问题。
- 使用方式: 审视前必须同时加载 MR/PR diff、目标分支源码上下文、专家绑定 Markdown 文档与本规范。
- 输出原则: 只输出本专家职责边界内的结论；发现跨边界风险时必须显式建议转交其他专家。

## 权威参考来源

- [Microsoft microservices guide: domain analysis and bounded context](https://learn.microsoft.com/azure/architecture/microservices/model/domain-analysis)
- [Spring Data domain events reference](https://docs.spring.io/spring-data/commons/reference/repositories/core-domain-events.html)
- [Spring Modulith Reference](https://docs.spring.io/spring-modulith/reference/)

## 职责边界

### 本专家必须负责

- 聚合边界、实体和值对象职责、领域服务和应用服务职责划分。
- bounded context 之间的模型边界、ACL、防腐层和共享语言。
- 仓储接口是否表达聚合意图，而不是暴露技术实现。
- 领域事件的产生、命名和边界含义是否清晰。

### 本专家不负责

- 不负责 SQL、索引、migration，这属于数据库专家。
- 不负责鉴权漏洞定性，这属于安全专家。
- 不负责纯代码可读性与命名微调，这属于可维护性专家。
- 不负责测试设计完整性，这属于测试专家。

### 遇到这些信号时必须移交

- 看到控制器/配置类越界时移交架构专家。
- 看到事务锁、表结构和查询问题时移交数据库专家。
- 看到权限和租户隔离时移交安全专家。
- 看到测试不能保护聚合不变量时移交测试专家。

## 固定审视流程

1. 先读 MR/PR 的目标 hunk，定位本次改动真正改了什么。
2. 再去目标分支源码仓检索同名方法、同名类型、调用方和被调用方。
3. 按本规范的维度逐条核对，只保留本专家职责边界内的直接证据。
4. 对证据不足的问题降级为待验证风险，不能因为经验感觉直接定性。
5. 修复建议必须说清楚怎么改、在哪改、哪些测试或验证要补齐。

## 源码仓检索策略

### 首选检索入口

- 搜索 aggregate、entity、value object、domain service、repository 接口。
- 搜索同一业务术语是否在多个模块出现不同模型定义。
- 搜索跨模块调用是否直接传递内部实体或枚举。
- 搜索 domain event、outbox、consumer 是否表达了清晰领域事实。
- 搜索 application service 是否吸收了本应属于聚合的不变量。

### 证据闭环要求

- 必须指出哪个领域概念被放错层，或者哪个聚合边界被穿透。
- 不能把普通分层问题都当成 DDD 问题，必须能映射到聚合、上下文或领域语言。
- 若只是代码风格更像贫血模型，但未破坏不变量，只能降级为 design_concern。
- 跨上下文问题必须至少给出两处模型或调用证据。

## 详细审视维度

### 维度 1: 聚合边界

- 维度目标: 确认不变量被聚合持有，跨聚合写操作不会偷偷发生。
- 重点搜索: aggregate、entity、repository、command handler、domain method。

#### 具体规则

##### 规则 1.1: 聚合根是否真正拥有关键不变量

- 在 MR 中重点看: 与“聚合边界”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“聚合边界”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“聚合边界”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 不变量落在聚合根内，跨聚合协作用应用服务或领域事件承接。
- 不良信号: 任何调用方都能改关键字段，聚合根形同虚设。
- 修复方向: 收口写入口到聚合根或聚合服务，并通过应用层协调跨聚合流程。

##### 规则 1.2: 是否存在绕过聚合根直接改子实体状态

- 在 MR 中重点看: 与“聚合边界”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“聚合边界”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“聚合边界”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 不变量落在聚合根内，跨聚合协作用应用服务或领域事件承接。
- 不良信号: 任何调用方都能改关键字段，聚合根形同虚设。
- 修复方向: 收口写入口到聚合根或聚合服务，并通过应用层协调跨聚合流程。

##### 规则 1.3: 跨聚合更新是否通过应用服务协调而非互相穿透

- 在 MR 中重点看: 与“聚合边界”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“聚合边界”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“聚合边界”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 不变量落在聚合根内，跨聚合协作用应用服务或领域事件承接。
- 不良信号: 任何调用方都能改关键字段，聚合根形同虚设。
- 修复方向: 收口写入口到聚合根或聚合服务，并通过应用层协调跨聚合流程。

##### 规则 1.4: 仓储接口是否以聚合意图为单位暴露能力

- 在 MR 中重点看: 与“聚合边界”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“聚合边界”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“聚合边界”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 不变量落在聚合根内，跨聚合协作用应用服务或领域事件承接。
- 不良信号: 任何调用方都能改关键字段，聚合根形同虚设。
- 修复方向: 收口写入口到聚合根或聚合服务，并通过应用层协调跨聚合流程。

#### 这一维度的高风险反模式

- repository 直接暴露子实体 update，让调用方绕过聚合根。
- 一个聚合同时承载多个独立业务生命周期。
- 跨聚合事务里随手改多个实体字段，没有显式协调逻辑。
- 聚合根只是数据袋，不负责任何不变量。

#### 这一维度的审查追问

- 哪个对象真正拥有这条业务规则？
- 如果跳过聚合根直接写字段，会不会破坏不变量？
- 这次改动是不是把两个聚合偷偷并成一个了？
- 仓储接口表达的是业务操作还是技术 CRUD？

### 维度 2: 领域服务与应用服务

- 维度目标: 确认应用服务只编排流程，领域服务只承载真正跨实体的领域规则。
- 重点搜索: application service、domain service、usecase、orchestrator。

#### 具体规则

##### 规则 2.1: 应用服务是否吞掉了本该在聚合内维护的不变量

- 在 MR 中重点看: 与“领域服务与应用服务”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“领域服务与应用服务”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“领域服务与应用服务”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 应用服务组织步骤，领域服务只保留真正的领域决策。
- 不良信号: service 名字是 domain，内容却全是技术和流程编排。
- 修复方向: 把不变量推回聚合，把跨聚合规则留给领域服务，把流程编排留给应用层。

##### 规则 2.2: 领域服务是否真的跨多个聚合或值对象才存在

- 在 MR 中重点看: 与“领域服务与应用服务”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“领域服务与应用服务”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“领域服务与应用服务”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 应用服务组织步骤，领域服务只保留真正的领域决策。
- 不良信号: service 名字是 domain，内容却全是技术和流程编排。
- 修复方向: 把不变量推回聚合，把跨聚合规则留给领域服务，把流程编排留给应用层。

##### 规则 2.3: 是否把技术校验、协议映射误塞进领域服务

- 在 MR 中重点看: 与“领域服务与应用服务”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“领域服务与应用服务”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“领域服务与应用服务”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 应用服务组织步骤，领域服务只保留真正的领域决策。
- 不良信号: service 名字是 domain，内容却全是技术和流程编排。
- 修复方向: 把不变量推回聚合，把跨聚合规则留给领域服务，把流程编排留给应用层。

##### 规则 2.4: 长 workflow 是否被误称为领域服务以规避设计问题

- 在 MR 中重点看: 与“领域服务与应用服务”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“领域服务与应用服务”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“领域服务与应用服务”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 应用服务组织步骤，领域服务只保留真正的领域决策。
- 不良信号: service 名字是 domain，内容却全是技术和流程编排。
- 修复方向: 把不变量推回聚合，把跨聚合规则留给领域服务，把流程编排留给应用层。

#### 这一维度的高风险反模式

- 领域服务里满是 HTTP DTO、事务注解和远端客户端。
- 应用服务有大量业务规则判断，却没有任何聚合方法。
- 为了避免改聚合，把所有规则都堆到 service 里。
- 一个所谓 domain service 同时编排消息、缓存和权限。

#### 这一维度的审查追问

- 这条规则属于某个聚合自身，还是跨聚合协作规则？
- 如果去掉 Spring 注解和远端调用，这个服务还剩下多少领域逻辑？
- 应用层是否只是组织步骤，还是已经在决定业务真相？
- 是否应该把规则推回实体/值对象？

### 维度 3: 上下文边界

- 维度目标: 确认 bounded context 没有通过内部模型直接耦合。
- 重点搜索: module、package、API contract、ACL、translator、anti-corruption layer。

#### 具体规则

##### 规则 3.1: 跨上下文是否直接传递内部 entity/enum

- 在 MR 中重点看: 与“上下文边界”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“上下文边界”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“上下文边界”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 上下文只通过公开契约交互，内部模型保持自治。
- 不良信号: 外部概念、枚举、异常直接渗入本域内部逻辑。
- 修复方向: 建立或补强 ACL/translator，停止跨上下文直接传实体。

##### 规则 3.2: 共享语言与内部实现模型是否被混用

- 在 MR 中重点看: 与“上下文边界”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“上下文边界”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“上下文边界”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 上下文只通过公开契约交互，内部模型保持自治。
- 不良信号: 外部概念、枚举、异常直接渗入本域内部逻辑。
- 修复方向: 建立或补强 ACL/translator，停止跨上下文直接传实体。

##### 规则 3.3: 防腐层是否存在且真正完成了翻译

- 在 MR 中重点看: 与“上下文边界”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“上下文边界”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“上下文边界”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 上下文只通过公开契约交互，内部模型保持自治。
- 不良信号: 外部概念、枚举、异常直接渗入本域内部逻辑。
- 修复方向: 建立或补强 ACL/translator，停止跨上下文直接传实体。

##### 规则 3.4: 一个上下文的变更是否逼迫另一个上下文同步重构

- 在 MR 中重点看: 与“上下文边界”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“上下文边界”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“上下文边界”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 上下文只通过公开契约交互，内部模型保持自治。
- 不良信号: 外部概念、枚举、异常直接渗入本域内部逻辑。
- 修复方向: 建立或补强 ACL/translator，停止跨上下文直接传实体。

#### 这一维度的高风险反模式

- 两个模块直接共享 entity 和 repository。
- 外部上下文的字段名直接渗进本域模型。
- 所谓 ACL 只是把外部 DTO 原样传进来。
- 上下文边界靠包名分隔，但运行时完全耦合。

#### 这一维度的审查追问

- 这里传递的是共享语言，还是内部实现模型？
- 另一个上下文改字段名时，这里会不会一起崩？
- 翻译层是否真的做了概念转换？
- 是否有必要把概念隔离成独立 contract？

### 维度 4: 领域事件

- 维度目标: 确认事件表达的是领域事实，而不是技术细节或半成品状态。
- 重点搜索: domain event、outbox、publisher、consumer、event naming。

#### 具体规则

##### 规则 4.1: 事件命名是否表达已经发生的业务事实

- 在 MR 中重点看: 与“领域事件”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“领域事件”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“领域事件”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 事件命名稳定、语义清晰、发布时间与业务提交一致。
- 不良信号: 把内部中间态当事件发出去，导致消费者绑定技术细节。
- 修复方向: 重命名事件、收缩载荷、把发布时间与提交边界对齐。

##### 规则 4.2: 事件载荷是否携带稳定领域语义而非内部实现细节

- 在 MR 中重点看: 与“领域事件”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“领域事件”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“领域事件”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 事件命名稳定、语义清晰、发布时间与业务提交一致。
- 不良信号: 把内部中间态当事件发出去，导致消费者绑定技术细节。
- 修复方向: 重命名事件、收缩载荷、把发布时间与提交边界对齐。

##### 规则 4.3: 事件发布时间是否与聚合提交语义一致

- 在 MR 中重点看: 与“领域事件”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“领域事件”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“领域事件”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 事件命名稳定、语义清晰、发布时间与业务提交一致。
- 不良信号: 把内部中间态当事件发出去，导致消费者绑定技术细节。
- 修复方向: 重命名事件、收缩载荷、把发布时间与提交边界对齐。

##### 规则 4.4: 消费者是否依赖了不该暴露的内部字段

- 在 MR 中重点看: 与“领域事件”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“领域事件”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“领域事件”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 事件命名稳定、语义清晰、发布时间与业务提交一致。
- 不良信号: 把内部中间态当事件发出去，导致消费者绑定技术细节。
- 修复方向: 重命名事件、收缩载荷、把发布时间与提交边界对齐。

#### 这一维度的高风险反模式

- 事件名字像 SaveDone、SyncTriggered 这类技术动作而非业务事实。
- 事件在事务未提交前发布，消费者读不到一致视图。
- 消费者强依赖 entity 全量快照。
- 事件字段没有语义说明，只是顺手把内部 DTO 全抛出去。

#### 这一维度的审查追问

- 这个事件是业务事实，还是技术过程中的中间动作？
- 消费者是否只依赖稳定领域字段？
- 如果事件重放，语义还能保持一致吗？
- 发布时机是否与事务提交一致？

### 维度 5: 仓储与领域语言

- 维度目标: 确认仓储接口和方法名表达业务语言，而不是裸技术 CRUD。
- 重点搜索: repository 接口、query object、specification、aggregate load/save。

#### 具体规则

##### 规则 5.1: 仓储方法是否以业务意图命名而不是技术动词堆砌

- 在 MR 中重点看: 与“仓储与领域语言”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“仓储与领域语言”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“仓储与领域语言”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 仓储围绕聚合意图建模，查询职责与报表职责分开。
- 不良信号: 仓储退化成全能 DAO，领域语言消失不见。
- 修复方向: 按聚合和用途拆仓储/查询接口，并统一领域术语。

##### 规则 5.2: 是否把跨聚合查询和报表查询硬塞进聚合仓储

- 在 MR 中重点看: 与“仓储与领域语言”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“仓储与领域语言”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“仓储与领域语言”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 仓储围绕聚合意图建模，查询职责与报表职责分开。
- 不良信号: 仓储退化成全能 DAO，领域语言消失不见。
- 修复方向: 按聚合和用途拆仓储/查询接口，并统一领域术语。

##### 规则 5.3: 仓储是否暴露了不该被外界依赖的内部查询细节

- 在 MR 中重点看: 与“仓储与领域语言”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“仓储与领域语言”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“仓储与领域语言”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 仓储围绕聚合意图建模，查询职责与报表职责分开。
- 不良信号: 仓储退化成全能 DAO，领域语言消失不见。
- 修复方向: 按聚合和用途拆仓储/查询接口，并统一领域术语。

##### 规则 5.4: 领域语言在仓储、服务和事件中是否前后一致

- 在 MR 中重点看: 与“仓储与领域语言”直接相关的新增字段、控制分支、配置、调用顺序和副作用变化。
- 去源码仓继续看: 围绕“仓储与领域语言”在目标分支继续检索调用方、被调用方、同类实现和历史约定。
- 直接证据要求: 需要证明“仓储与领域语言”在真实运行链路中会出错、退化或失真，不能只凭经验下结论。
- 良好信号: 仓储围绕聚合意图建模，查询职责与报表职责分开。
- 不良信号: 仓储退化成全能 DAO，领域语言消失不见。
- 修复方向: 按聚合和用途拆仓储/查询接口，并统一领域术语。

#### 这一维度的高风险反模式

- 仓储接口像万能 DAO，任何表都往里塞。
- 报表查询和聚合加载写在同一个 repository。
- 方法名是 findByXAndYAndZ，但没人知道业务意图。
- 同一业务概念在不同层分别叫三种名字。

#### 这一维度的审查追问

- 仓储方法名能否直接让领域专家听懂意图？
- 这条查询是聚合加载，还是报表/投影需求？
- 为什么这个仓储要暴露底层技术细节？
- 领域术语是否在接口、事件和文档中保持一致？

## 常见误报与越界提醒

- 不要把所有分层问题都贴上 DDD 标签，只有聚合、上下文、领域语言失真才算。
- 不要因为实体没有很多方法就直接判贫血模型，先看不变量是否真的丢失。
- 不要把普通事件总线使用问题误判为领域事件设计问题。
- 不要代替架构专家评价所有模块依赖，只看 bounded context 与聚合边界。
- 不要代替数据库专家评价仓储里的 SQL 细节。

## Java/Spring 场景化检查清单

- 检查 Spring Modulith 或模块边界是否与 bounded context 一致。
- 检查 repository 是否围绕 aggregate load/save 建模。
- 检查 domain event 是否在事务提交语义后发布。
- 检查 application service 是否只编排 command，而非承载核心不变量。
- 检查 entity/value object 是否拥有真正的领域表达力。

## 典型评审场景

### 场景 1: 子实体被外部直接 update

- 典型改动: MR 新增 repository 方法直接更新子实体字段。
- 第一反应不要做什么: 不要只批评抽象不优雅，要指出这会绕过哪个聚合不变量。
- 应该先去看的代码: 搜聚合根方法、子实体关系和所有写入口。
- 结论成立的关键证据: 证明外部调用可以在不经过聚合根的情况下破坏规则。
- 常见错误结论: “Repository 不该 update”这种空泛意见。
- 更好的修复建议写法: 建议把写入口收回聚合根或领域服务，并保留显式意图方法。

### 场景 2: 跨上下文共享内部枚举

- 典型改动: MR 让两个模块直接复用同一内部 enum。
- 第一反应不要做什么: 不要只说依赖不好，要说明上下文语言被绑定了。
- 应该先去看的代码: 搜 enum 定义、外部契约和 translator/ACL。
- 结论成立的关键证据: 指出另一个上下文被迫理解本域内部状态的具体位置。
- 常见错误结论: “枚举共享一定有问题”。
- 更好的修复建议写法: 建议引入稳定契约或 translator，而不是直接共享内部模型。

### 场景 3: 应用服务吸走聚合规则

- 典型改动: MR 在 service 里加入大量状态判断和不变量校验。
- 第一反应不要做什么: 不要只说方法太长，要指出领域规则拥有者错了。
- 应该先去看的代码: 搜聚合方法、原有不变量和 service 调用链。
- 结论成立的关键证据: 证明规则无法被实体自身维护，导致多入口不一致。
- 常见错误结论: “Service 里有 if 就不好”。
- 更好的修复建议写法: 建议把核心规则推回聚合根，把 service 收敛成编排层。

### 场景 4: 事件表达技术动作而非领域事实

- 典型改动: MR 新增某个 save/sync/update 事件。
- 第一反应不要做什么: 不要只盯命名，要看消费者绑定了什么语义。
- 应该先去看的代码: 搜发布点、消费者、事件字段和事务边界。
- 结论成立的关键证据: 指出消费者在依赖内部过程，而不是稳定业务事实。
- 常见错误结论: “名字不好看”。
- 更好的修复建议写法: 建议改成领域事实事件，并收缩到稳定字段。

### 场景 5: 仓储变成万能 DAO

- 典型改动: MR 给聚合仓储继续塞入报表和联表查询。
- 第一反应不要做什么: 不要只从代码量角度评价，要指出领域语言被破坏。
- 应该先去看的代码: 查看方法命名、返回类型和调用方用途。
- 结论成立的关键证据: 证明仓储已经无法表达聚合意图，只剩技术查询。
- 常见错误结论: “方法太多不好”。
- 更好的修复建议写法: 建议拆出读模型查询接口，保留聚合仓储纯净性。

### 场景 6: 共享语言与内部模型混用

- 典型改动: MR 把外部请求 DTO 直接塞进领域对象。
- 第一反应不要做什么: 不要只说耦合，要指出概念翻译缺失。
- 应该先去看的代码: 查看 DTO、entity、translator 和应用服务边界。
- 结论成立的关键证据: 证明领域模型开始依赖外部协议字段。
- 常见错误结论: “DTO 和 entity 不能同名”。
- 更好的修复建议写法: 建议加翻译层并恢复领域术语纯度。

## 结论输出格式

- 命中的规范条款: 只列本专家真正命中的维度与规则编号。
- 问题定性: 在 `direct_defect / risk_hypothesis / test_gap / design_concern` 中选择一种，不要混写。
- 代码定位: 至少给出文件、方法或类名，以及对应的 diff hunk 或目标分支上下文位置。
- 证据说明: 先写 MR 片段证据，再写源码仓补充证据，缺一不可时要降级。
- 影响说明: 说明问题会影响哪类请求、哪条链路、哪种数据或哪类调用。
- 修复建议: 说明具体改法、推荐落点、需要一起修改的关联代码。
- 验证建议: 指出需要补的测试、脚本、回放或人工核对步骤。
- 移交建议: 若问题跨界，明确应该拉哪个专家继续看。

## 跨文件核对清单

- 当前 MR 修改的主文件是否只是入口，真正语义是否落在其他类、配置或转换层？
- 当前字段、状态、枚举、事件名是否在目标分支还有其他读写路径？
- 当前改动是否同步更新了异步消费者、批任务、报表、缓存或投影？
- 当前改动是否引入了新的前置条件、默认值或失败路径？
- 当前改动是否要求调用方、下游或历史数据同步迁移？
- 当前改动是否影响日志、审计、监控、告警或回放工具对链路的理解？
- 当前改动是否会在重试、并发、回滚、补偿、回放场景下产生不同结果？
- 当前改动是否只是把问题从主链路挪到其他任务、事件或缓存链路里？
- 当前改动若上线一半，系统是否仍然保持兼容和可恢复？
- 当前改动若被回滚，是否存在残留状态、脏数据或契约不兼容？

## 修复建议写法

- 先写“要改什么”，再写“为什么要这样改”，最后写“如何验证改对了”。
- 修复建议必须指出推荐落点，例如 aggregate、service、repository、migration、consumer、test。
- 若需要联动修改多个文件，要列出主改动点和至少一个关键联动点。
- 若问题涉及兼容性，要明确灰度、补数、回滚或双写双读策略。
- 若问题涉及时间、金额、幂等、权限、缓存、消息等高风险语义，要指出具体样例。
- 不要只写“建议重构”“建议优化”“建议补校验”这类无落点的空话。
- 建议代码修改方案应优先贴近当前项目技术栈和已存在实现，而不是重新发明框架。
- 若证据不足，修复建议应写成“补验证+补保护”，而不是强行给出大改方案。

## 快速口头判断禁令

- 禁止只因为看见 import、注解、字段名、目录名就直接下结论。
- 禁止只读单个 diff hunk 就宣称已经理解整条业务链路。
- 禁止把框架偏好、个人习惯或历史写法直接当成缺陷证据。
- 禁止在没有目标分支源码上下文时给高置信结论。
- 禁止把属于其他专家的风险强行写成自己的正式问题。
- 禁止只给“建议优化”却不写具体落点和验证方式。
- 禁止把旧代码已有问题全部记到本次 MR 头上，除非 MR 明确放大了风险。
- 禁止把偶发猜测写成必现缺陷，必须说明触发条件。
- 禁止把测试缺口直接等同于线上缺陷，除非已经有直接业务错误证据。
- 禁止在缺少兼容性证据时随意建议大规模重构或重写。
- 禁止忽略异步链路、回放、补偿、批任务和报表这类非主链路使用点。
- 禁止在修复建议里绕过现有架构和领域边界重新发明一套实现。

## 跨专家协作提示

- 当问题同时涉及两个以上领域时，先给出本专家职责内的主结论，再明确建议转交对象。
- 若某条问题的直接根因不在本专家边界内，不要抢结论，只能给出“我观察到的上游/下游信号”。
- 与架构专家协作时，重点提供本领域的直接证据，不要代替其判断总体边界。
- 与正确性专家协作时，重点提供技术语义如何放大业务错误，而不是重写业务定义。
- 与数据库专家协作时，重点提供缓存、消息、事务、契约如何影响数据库链路的补充证据。
- 与安全专家协作时，重点提供实际攻击面或数据边界线索，而不是含糊写成“有风险”。
- 与测试专家协作时，重点指出最需要保护的失败路径和最小可复现样例。
- 若问题已足以由本专家单独闭环，不要为了显得全面而硬拉其他专家。
- 若只是怀疑别的领域可能也有问题，要写成“建议进一步检查”，不要直接替他人定性。
- 在最终结论里，优先保留本专家最强的一到两条问题，而不是平铺大量弱结论。

## 证据引用模板

- MR 证据模板: `文件 + hunk + 当前改动前后差异 + 触发条件`。
- 源码仓证据模板: `目标分支文件 + 调用方/被调用方 + 现有实现假设`。
- 风险说明模板: `在什么输入/时序/负载/权限前提下会出错`。
- 修复建议模板: `修改落点 + 关键改法 + 联动文件 + 验证步骤`。
- 若是跨文件问题，必须至少列出“主修改点”和“至少一个受影响上下游文件”。
- 若是兼容性问题，必须列出“旧数据/旧缓存/旧消息/旧调用方”中至少一个实际受影响对象。
- 若是并发问题，必须列出“竞争窗口”或“顺序假设”。
- 若是权限问题，必须列出“攻击者或越权主体”与“被访问对象”。

## 结论自检

- 如果把专家名称遮住，这条结论还能看出明确的职责边界吗？
- 如果删掉源码仓上下文，这条结论是否就会失去关键证据？若会，说明引用方式是对的。
- 如果把修复建议交给开发同学，他是否能直接知道下一步要改哪里、补什么验证？

## 合并前最终检查

- 是否明确指出了聚合、上下文或领域语言具体哪里失真？
- 是否避免把普通分层问题误报成 DDD 问题？
- 是否给出了应移交给其他专家的信号？
- 是否说明了更合理的聚合或上下文边界？
- 是否给出了领域层面的修复建议而不是纯技术重构口号？

## 术语表

- 聚合: 共同维护一组业务不变量的对象边界。
- bounded context: 拥有独立模型和术语的上下文边界。
- 共享语言: 团队和模型共同认可的业务术语体系。
- ACL: 保护本上下文不被外部模型污染的翻译层。
- 领域事件: 已经发生的业务事实，而不是技术过程中的瞬时动作。
